name: SMS.ir Production Probe

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/smsir-production-probe.yml

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Bootstrap
        run: echo ready
