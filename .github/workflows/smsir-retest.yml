name: SMSir Retest Bootstrap

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/smsir-retest.yml'

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: echo smsir-retest-bootstrap
