import fs from 'node:fs';
const read=p=>fs.readFileSync(p,'utf8');
const expect=(ok,msg)=>{if(!ok)throw new Error(`Contract exit/job-ad/user controls validation failed: ${msg}`)};
const control=read('worker/contract-exit-job-ad-user-controls-v1.ts');
const reopen=read('worker/staff-contract-reopen-v1.ts');
const productionRepair=read('worker/contract-production-repair-v1.ts');
const adminRowGuarantee=read('worker/admin-contract-row-guarantee-v1.ts');
const contractHistoryRepair=read('worker/contract-history-repair-v1.ts');
const applicationLifecycle=read('worker/job-application-lifecycle-v1.ts');
const lifecycleMigration=read('migrations/0112_job_application_lifecycle_status.sql');
const legacy=read('worker/legacy-contract-compat-v1.ts');
const decorate=read('worker/legacy-job-ad-decoration-v1.ts');
const listPoints=read('worker/contract-list-points-v1.ts');
const entry=read('worker/index-desktop-react-v1.ts');
const engine=read('worker/contract-progress-engine-v1.ts');
const users=read('desktop-react/users-access-v3.tsx');
const desktop=read('desktop-react/job-ads-v3.tsx');
const desktopOwner=read('desktop-react/job-ads-v1.tsx');
const contractsOwner=read('desktop-react/contracts-lifecycle-v2.tsx');
const contracts=read('desktop-react/contracts-lifecycle-v5.tsx');
const contractsCss=read('desktop-react/contracts-lifecycle-v5.css');
const mobile=read('mobile-react/admin-job-ads-v4.tsx');
const mobileOwner=read('mobile-react/admin-job-ads-v3.tsx');
const caregiverBank=read('mobile-react/caregiver-job-ads-v1.tsx');
const classicBridge=read('preview/desktop-react-entry-bridge-v1.js');

expect(entry.includes('routeContractExitJobAdUserControlsV1')&&!entry.includes('routeCaregiverContractWithdrawHotfix'), 'old caregiver withdrawal hotfix still owns the route');
expect(control.includes("status='ENDED_EARLY'")&&control.includes("status='WITHDRAWN'")&&control.includes('"PUBLISHED","WITHDRAWN"'), 'caregiver withdrawal does not end contract and immediately republish the ad');
expect(engine.includes('if(row.pointsModel==="LEGACY_PREPAID")')&&control.includes('reconcileAllActiveContracts'), 'legacy prepaid points are not preserved through canonical reconciliation');
expect(control.includes('existing.status!=="WITHDRAWN"')&&control.includes("status='PENDING_CONSULTANT'"), 'withdrawn caregiver cannot reapply after ad reopening');
expect(reopen.includes('reopenMode')&&reopen.includes('PUBLISH')&&reopen.includes('EDIT')&&reopen.includes("end_reason_code='STAFF_REMOVAL'"), 'staff contract removal does not expose publish/edit choices');
expect(reopen.includes('body?.reopenMode??body?.mode'), 'contract-exit endpoint does not accept both current and cached/legacy client payload names');
expect(desktop.includes('reopenMode:mode'), 'current React job-ad UI is not sending the canonical contract-exit payload');
expect(reopen.includes('prepareContractForStaffExit')&&reopen.includes('legacyApplicationByAd')&&reopen.includes('legacyApplicationOnly')&&reopen.includes('futurePointsStopped:true'), 'staff removal is not resilient for application-only legacy contracts');
expect(reopen.includes("UPDATE care_job_applications SET status='WITHDRAWN'")&&reopen.includes("published_at=CASE WHEN ?='PUBLISHED' THEN ? ELSE published_at END"), 'staff removal does not hide the old caregiver and freshly republish the ad');
expect(applicationLifecycle.includes('lifecycle_status')&&applicationLifecycle.includes('applicationStorageStatus')&&applicationLifecycle.includes('"REJECTED"'), 'additive application lifecycle compatibility layer is missing');
expect(lifecycleMigration.includes('ADD COLUMN lifecycle_status')&&lifecycleMigration.includes('SET lifecycle_status=status')&&!/DROP\s+TABLE/i.test(lifecycleMigration), 'production lifecycle migration is not additive and data-safe');
expect(entry.indexOf('const productionContractResponse=await routeProductionContractRepairV1')<entry.indexOf('const reopenResponse=await routeStaffContractReopenV1'), 'production-safe contract route does not own contract-exit before the legacy handler');
expect(productionRepair.includes('COALESCE(ap.lifecycle_status,ap.status)')&&productionRepair.includes('lifecycleUpdateStatement(env,target.applicationId,"WITHDRAWN"')&&productionRepair.includes('contractRowGuaranteed:true'), 'production route does not bypass the historical application-status CHECK while guaranteeing a canonical contract row');
expect(productionRepair.includes('reconcileCanonicalOpenContractRows')&&productionRepair.includes('reconcileHistoricalContractRows')&&productionRepair.includes('reconcileMissingAdminContractRows'), 'live contract list does not repair open, historical, and missing admin contract rows');
expect(productionRepair.includes('guaranteeAdminCase')&&productionRepair.includes('admin_contract_row_persistence_failed')&&productionRepair.includes('contract_history_materialization_required'), 'IN_CONTRACT and contract exit do not enforce an administrator-visible contract row');
expect(adminRowGuarantee.includes('ensureAdminContractRowForApplication')&&adminRowGuarantee.includes('admin_contract_fallback_persistence_failed')&&adminRowGuarantee.includes('SELECT id FROM contract_cases_v3 WHERE job_contract_id=? LIMIT 1'), 'admin contract row fallback is not persisted and verified by canonical contract id');
expect(contractHistoryRepair.includes("COALESCE(ap.lifecycle_status,ap.status)='IN_CONTRACT'")&&contractHistoryRepair.includes("IN ('WITHDRAWN','COMPLETED')")&&contractHistoryRepair.includes('ensureHistoricalContractForApplication'), 'application-only contract history is not backfilled before/after exit');
expect(productionRepair.includes('contract_exit_persistence_failed')&&productionRepair.includes('contract_row_persistence_failed'), 'production persistence failures do not return diagnostic error codes');
expect(legacy.includes('COALESCE(a.reward_points,a.contract_points,0)')&&legacy.includes('LEGACY_PREPAID')&&legacy.includes('DAILY_V1'), 'legacy contracts do not preserve allocated point logic');
expect(legacy.includes('INSERT OR IGNORE INTO caregiver_contract_point_daily_ledger')&&legacy.includes('reconcileLegacyOpenContracts'), 'legacy contracts are not idempotently backfilled into daily point accrual');
expect(entry.includes('reconcileLegacyOpenContracts')&&entry.includes('decorateLegacyJobAdContractState'), 'active production entrypoint does not own legacy backfill and ad decoration');
expect(decorate.includes("status='IN_CONTRACT'")&&decorate.includes('hasActiveContract=true'), 'historical active contracts are not recognized in staff job-ad responses');
expect(control.includes('active_contract_blocks_edit')&&control.includes('active_contract_blocks_delete')&&control.includes("status='DELETED'"), 'job ad edit/delete safety guards or soft delete are missing');
expect(control.includes('deleteAccountV2')&&control.includes("recruitment_stage='DELETED'")&&control.includes('active_contract_blocks_user_delete'), 'account + caregiver profile safe deletion is incomplete');
expect(users.includes('ابتدا نقش حساب را انتخاب کنید')&&users.includes('panel==="CAREGIVER"')&&users.includes('panel="STAFF"')&&users.includes('حذف حساب و پروفایل'), 'role-first permission matrix or delete option is missing');
expect(desktopOwner.includes('./job-ads-v4')&&desktop.includes('خلع و انتشار مجدد')&&desktop.includes('خلع و ویرایش آگهی')&&desktop.includes('اصلاح آگهی')&&desktop.includes('حذف آگهی'), 'desktop job ad controls are incomplete');
expect(desktop.includes('<th>امتیاز</th>')&&desktop.includes('{en(ad.contractPoints)} امتیاز'), 'desktop staff job-ad bank does not visibly show allocated points');
expect(mobileOwner.includes('./admin-job-ads-v4')&&mobile.includes('خلع و انتشار مجدد')&&mobile.includes('خلع و ویرایش آگهی')&&mobile.includes('اصلاح')&&mobile.includes('حذف'), 'mobile job ad controls are incomplete');
expect(mobile.includes('maj-points')&&mobile.includes('{fa(ad.contractPoints)} امتیاز'), 'mobile admin job-ad bank does not visibly show allocated points');
expect(caregiverBank.includes('ad.contractPoints')&&caregiverBank.includes('امتیاز'), 'caregiver job-ad bank lost allocated-point visibility');
expect(entry.includes('decorateContractListPointsV1')&&listPoints.includes('earnedPoints')&&listPoints.includes('remainingPoints')&&listPoints.includes('x-salamat-contract-list-points'), 'contract list API does not expose earned and remaining points');
expect(contractsOwner.includes('./contracts-lifecycle-v6'), 'contract lifecycle Jalali V6 is not the active desktop owner');
expect(contracts.includes('ContractMeter')&&contracts.includes('امتیاز رفته')&&contracts.includes('امتیاز باقی‌مانده')&&contracts.includes('مراقب / ستاره'), 'contract rows do not show caregiver stars, remaining days and point meter data');
expect(['اطلاعات اعزام','لیست خدمت‌دهندگان','اطلاعات نظارت قرارداد','اطلاعات مالی و اعتباری قرارداد'].every(x=>contracts.includes(x)), 'contract detail lost one of the four exact tabs');
expect(contractsCss.includes('.clv5-meter-track')&&contractsCss.includes('.clv5-meter.RENEW_NOW')&&contractsCss.includes('.clv5-meter.NEAR_RENEWAL'), 'bounded color-coded contract meter styles are missing');
expect(entry.includes('"/panel/index.html"')&&entry.includes('CLASSIC_REACT_BRIDGE'), 'authenticated users can still remain trapped on the stale classic panel entry');
expect(classicBridge.includes("'/app/job_ads'")&&classicBridge.includes("'/app/contracts'")&&classicBridge.includes('salamat-authenticated'), 'classic panel does not hand active staff sessions to the current React job-ad/contracts owner');
console.log('Production contract exit and IN_CONTRACT preserve a durable administrator-visible contract row, including multi-period V3 history.');
