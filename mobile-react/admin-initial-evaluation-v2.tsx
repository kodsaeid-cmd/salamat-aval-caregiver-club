import React,{useEffect,useState} from "react";
import {ArrowRight,ChevronLeft,Search,ShieldCheck,UserCheck} from "lucide-react";
import {InitialEvaluationFormV2,InitialNotify} from "../shared/initial-evaluation-v2";
import "./admin-evaluations-v2.css";
import "./admin-evaluations-v3.css";

async function api(path:string){const response=await fetch(path,{credentials:"same-origin",cache:"no-store"}),raw=await response.text();let payload:any={};try{payload=raw?JSON.parse(raw):{}}catch{}if(!response.ok)throw new Error(payload.message||`خطای ${response.status}`);return payload}
const text=(value:unknown,fallback="—")=>String(value??"").trim()||fallback;
const fa=(value:unknown)=>Number(value||0).toLocaleString("fa-IR");
const initials=(name:unknown)=>text(name,"م").split(/\s+/).filter(Boolean).map(x=>x[0]).join("").slice(0,2)||"م";
export function AdminInitialEvaluationMobileV2({notify}:{notify:InitialNotify}){
 const [access,setAccess]=useState<any>(null),[rows,setRows]=useState<any[]>([]),[query,setQuery]=useState(""),[selected,setSelected]=useState<any>(null),[loading,setLoading]=useState(false),[error,setError]=useState(""),[page,setPage]=useState(1),[pagination,setPagination]=useState<any>({});
 useEffect(()=>{void api("/api/staff/initial-evaluations/access").then(p=>setAccess(p.data||null)).catch((e:any)=>{setAccess({allowed:false});setError(e.message);notify(e.message,"error")})},[]);
 const load=async(next=page,q=query)=>{setLoading(true);setError("");try{const params=new URLSearchParams({page:String(next),pageSize:"25"});if(q.trim())params.set("q",q.trim());const p=await api(`/api/admin/caregivers-page?${params}`),data=p.data||{};setRows(Array.isArray(data.items)?data.items:[]);setPagination(data.pagination||{});setPage(next)}catch(e:any){setRows([]);setError(e.message);notify(e.message,"error")}finally{setLoading(false)}};
 useEffect(()=>{if(access?.allowed)void load(1,"")},[access?.allowed]);
 if(access===null)return <div className="mae-state"><span className="mae-spinner"/><strong>در حال بررسی دسترسی ارزیابی بدوی...</strong></div>;
 if(!access?.allowed)return <div className="ma-card" style={{display:"flex",gap:9,alignItems:"flex-start"}}><ShieldCheck size={20}/><div><strong>بخش محرمانه ارزیابی بدوی</strong><p>استفاده از این فرم نیازمند اختیار مدیر سامانه است.</p>{error&&<small>{error}</small>}</div></div>;
 if(selected)return <div className="mae-drill"><header className="mae-mobile-head"><button type="button" onClick={()=>{setSelected(null);window.scrollTo({top:0,behavior:"auto"})}}><ArrowRight size={20}/></button><strong>ارزیابی بدوی</strong><span/></header><main className="mae-workspace"><section className="mae-person-head"><div className="mae-avatar">{initials(selected.fullName)}</div><div><small>فرم محرمانه ارزیابی بدوی</small><h2>{text(selected.fullName,"مراقب")}</h2><p>{text(selected.membershipCode)} • {text(selected.mobile)}</p></div><UserCheck size={22}/></section><InitialEvaluationFormV2 caregiverId={selected.id} initialAccess={access} notify={notify}/></main></div>;
 const totalPages=Math.max(1,Number(pagination.totalPages||pagination.pages||1)),total=Number(pagination.total||rows.length);
 return <div className="mae-main"><form className="mae-search" onSubmit={event=>{event.preventDefault();void load(1,query)}}><Search size={18}/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="نام، پرونده، موبایل یا کد ملی"/></form>{error?<div className="mae-state error"><strong>دریافت فهرست انجام نشد</strong><small>{error}</small><button onClick={()=>void load(page,query)}>تلاش مجدد</button></div>:loading?<div className="mae-state"><span className="mae-spinner"/><strong>در حال دریافت فهرست مراقبین...</strong></div>:<><div className="mae-caregiver-list">{rows.map((caregiver:any)=><button type="button" key={caregiver.id} onClick={()=>{setSelected(caregiver);window.scrollTo({top:0,behavior:"auto"})}}><span className="mae-avatar small">{initials(caregiver.fullName)}</span><div><strong>{text(caregiver.fullName,"مراقب")}</strong><small>{text(caregiver.membershipCode)} • {text(caregiver.mobile)}</small></div><span className="mae-row-level">{text(caregiver.professionalLevel,"بدوی")}</span><ChevronLeft size={18}/></button>)}</div><div className="mae-pager"><button disabled={page<=1} onClick={()=>void load(page-1,query)}>قبلی</button><span>{fa(total)} مراقب • {fa(page)} از {fa(totalPages)}</span><button disabled={page>=totalPages} onClick={()=>void load(page+1,query)}>بعدی</button></div></>}</div>;
}
