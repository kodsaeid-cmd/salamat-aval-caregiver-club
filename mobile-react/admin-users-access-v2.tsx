import React,{useEffect,useState} from "react";
import {MobileAdminUsersAccessV1} from "./admin-users-access-v1";
import {api,fa,Notify} from "../desktop-react/core";
import "./admin-users-access-v2.css";

export function MobileAdminUsersAccessV2({access,notify}:{access:any;notify:Notify}){const [summary,setSummary]=useState({pending:0,newJoiners:0});const load=async()=>{try{const p:any=await api("/api/users?page=1&pageSize=250");const rows=Array.isArray(p.data)?p.data:[];setSummary({pending:Number(p.meta?.pendingCaregiverApprovals??rows.filter((x:any)=>x.pendingApproval||String(x.status).toUpperCase()==="PENDING").length),newJoiners:Number(p.meta?.newNetworkJoiners??rows.filter((x:any)=>x.newJoiner||x.pendingApproval).length)})}catch{}};useEffect(()=>{void load();const id=window.setInterval(load,15000);return()=>window.clearInterval(id)},[]);return <div className="mau2-wrap"><section className="mau2-network-card"><article><small>پیوسته به شبکه مراقبین</small><strong>{fa(summary.newJoiners)}</strong><span>ثبت‌نام مستقیم فرم</span></article><article><small>در انتظار فعال‌سازی</small><strong>{fa(summary.pending)}</strong><span>ورود تا تأیید مدیر مسدود است</span></article></section><MobileAdminUsersAccessV1 access={access} notify={notify}/></div>}
