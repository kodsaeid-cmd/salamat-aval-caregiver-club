(()=>{
'use strict';

// Retired caregiver finance renderer.
// This filename is intentionally kept as a harmless compatibility asset so
// cached or historical HTML can never resurrect the old DOM owner.
window.__salamatUnifiedFinancialV4=true;
window.SalamatUnifiedFinancialV4LegacyRetired=Object.freeze({
  version:'4.0.1-retired',
  retired:true,
  reason:'superseded-by-server-financial-profile-v4',
});
})();
