(()=>{
'use strict';
if(window.__salamatUnifiedFinancialV4)return;
window.__salamatUnifiedFinancialV4=true;

const VERSION='4.0.0';
const ENDPOINT='/api/caregiver/platform/financial-profile';
const $=(s,r=document)=>r.querySelector(s);
const $$=(s,r=document)=>[...r.querySelectorAll(s)];
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const fa=v=>Number(v||0).toLocaleString('fa-IR',{maximumFractionDigits:1});
const money=v=>`${Number(v||0).toLocaleString('fa-IR')} تومان`;
const pct=v=>Math.max(0,Math.min(100,Number(v||0)));
const state={data:null,promise:null,lastLoadedAt:0,timer:0,activeTab:'retention',busy:false,observers:[]};
const statusLabel={ELIGIBLE:'واجد شرایط بررسی',IN_PROGRESS:'در حال تکمیل سابقه',PAUSED:'شمارش سابقه متوقف است',NO_CONTRACTS:'قرارداد معتبر ثبت نشده',WAITING_EVALUATION:'منتظر ارزیابی نهایی',SCORE_BELOW_THRESHOLD:'امتیاز کمتر از حد لازم',REQUESTED:'ثبت درخواست',UNDER_REVIEW:'در حال بررسی',APPROVED:'تأیید شده',REJECTED:'رد شده',CANCELLED:'لغو شده',PAID:'پرداخت شده',COMPLETED:'معرفی موفق'};

async function api(path,options={}){const headers=new Headers(options.headers||{});if(typeof options.body==='string'&&!headers.has('content-type'))headers.set('content-type','application/json');const response=await fetch(path,{credentials:'same-origin',cache:'no-store',...options,headers});const text=await response.text();let payload={};try{payload=text?JSON.parse(text):{}}catch{payload={message:text}}if(!response.ok){const error=new Error(payload.message||`خطای ${response.status}`);error.code=payload.error;throw error}return payload}
function toast(a,b){try{window.toast?.(a,b)}catch{}if(!window.toast)console.info(a,b)}
function currentUser(){try{return window.SalamatBackend?.getCurrentUser?.()||window.__salamatPanelAccessV2?.user||null}catch{return null}}
function isCaregiver(){return String(currentUser()?.role||'').toUpperCase()==='CAREGIVER'||String($('#sidebarRole')?.textContent||'').includes('مراقب')}
function activeModule(){return $('#sidebarNav [data-caregiver-module-key].active')?.dataset?.caregiverModuleKey||$('#sidebarNav [data-panel-module-key].active')?.dataset?.panelModuleKey||''}
function isWallet(){const key=activeModule(),title=String($('#pageTitle')?.textContent||'');return isCaregiver()&&(key==='caregiver.wallet'||/کیف پول|اعتبار/.test(title))}
function dateFa(value){if(!value)return '—';try{return new Intl.DateTimeFormat('fa-IR-u-ca-persian',{year:'numeric',month:'short',day:'numeric'}).format(new Date(value))}catch{return esc(value)}}
function score(value){return value===null||value===undefined?'—':fa(value)}
function duration(item){const months=Number(item?.months||0),days=Number(item?.days||0);return `${fa(months)} ماه${days?` و ${fa(days)} روز`:''}`}
function metricProgress(item){const metric=Number(item?.evaluation?.metric),threshold=Number(item?.scoreThreshold||item?.evaluation?.threshold||0);if(!Number.isFinite(metric)||!threshold)return 0;return pct(metric/threshold*100)}
function eligibilityProgress(item){return Math.min(pct(item?.progressPercent),metricProgress(item))}
function comparison(item){return item?.comparison==='GT'?`بیشتر از ${fa(item?.scoreThreshold)}`:`حداقل ${fa(item?.scoreThreshold)}`}
function hasOpenRequest(data){return (data?.wallet?.creditRequests||[]).some(x=>['REQUESTED','UNDER_REVIEW','APPROVED'].includes(String(x.status)))}

function addStyles(){if($('#ufpV4Styles'))return;const style=document.createElement('style');style.id='ufpV4Styles';style.textContent=`
.ufp{direction:rtl;display:grid;gap:14px}.ufp *{box-sizing:border-box}.ufp-head{padding:18px 20px;border:1px solid #dce8e2;border-radius:20px;background:linear-gradient(135deg,#f7fcf9,#eef8f3);display:flex;justify-content:space-between;gap:14px;align-items:center;flex-wrap:wrap}.ufp-head h2{margin:0;font-size:18px;color:#173e2d}.ufp-head p{margin:6px 0 0;color:#718179;font-size:9px;line-height:1.9}.ufp-tabs{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:8px}.ufp-tab{border:1px solid #dce7e1;background:#fff;border-radius:14px;padding:11px 8px;font:inherit;font-size:9px;font-weight:900;color:#51675c;cursor:pointer}.ufp-tab.active{background:#087a45;color:#fff;border-color:#087a45}.ufp-panel{display:none}.ufp-panel.active{display:grid;gap:13px}.ufp-card{background:#fff;border:1px solid #dce8e2;border-radius:19px;box-shadow:0 9px 28px rgba(20,70,45,.045);overflow:hidden}.ufp-card-head{padding:15px 17px;border-bottom:1px solid #edf2ef;display:flex;justify-content:space-between;gap:12px;align-items:flex-start;flex-wrap:wrap}.ufp-card-head h3{margin:0;font-size:13px;color:#173e2d}.ufp-card-head p{margin:5px 0 0;color:#74857c;font-size:8px;line-height:1.8}.ufp-body{padding:16px}.ufp-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:11px}.ufp-grid.two{grid-template-columns:repeat(2,minmax(0,1fr))}.ufp-gauge-card{padding:15px;border:1px solid #e2ebe6;border-radius:16px;background:#fbfdfc;display:grid;grid-template-columns:auto minmax(0,1fr);gap:14px;align-items:center}.ufp-gauge{--p:0;width:116px;aspect-ratio:1;border-radius:50%;background:conic-gradient(#078848 calc(var(--p)*1%),#e5eee9 0);display:grid;place-items:center;position:relative}.ufp-gauge:after{content:'';position:absolute;inset:11px;border-radius:50%;background:#fff}.ufp-gauge>div{position:relative;z-index:1;text-align:center}.ufp-gauge strong{display:block;color:#087a45;font-size:20px}.ufp-gauge small{display:block;margin-top:3px;color:#74857c;font-size:7px}.ufp-gauge-copy h4{margin:0;font-size:11px;color:#263d32}.ufp-gauge-copy p{margin:6px 0 0;color:#74857c;font-size:8px;line-height:1.9}.ufp-badge{display:inline-flex;margin-top:7px;padding:5px 8px;border-radius:999px;background:#edf8f2;color:#087747;font-size:7px;font-weight:900}.ufp-badge.warn{background:#fff4dd;color:#91620b}.ufp-badge.danger{background:#ffedf0;color:#a82a41}.ufp-kpis{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:9px}.ufp-kpi{padding:12px;border:1px solid #e1ebe6;border-radius:14px;background:#fbfdfc}.ufp-kpi small{display:block;color:#788880;font-size:7px}.ufp-kpi strong{display:block;margin-top:6px;color:#087a45;font-size:15px}.ufp-actions{display:flex;gap:8px;flex-wrap:wrap;margin-top:11px}.ufp-btn{border:0;border-radius:10px;background:#edf8f2;color:#087747;padding:9px 12px;font:inherit;font-size:8px;font-weight:900;cursor:pointer}.ufp-btn.primary{background:#087a45;color:#fff}.ufp-btn.danger{background:#ffedf0;color:#a82a41}.ufp-btn:disabled{opacity:.5;cursor:not-allowed}.ufp-guide{overflow:auto}.ufp-table{width:100%;border-collapse:collapse;min-width:720px}.ufp-table th,.ufp-table td{padding:10px 11px;text-align:right;border-bottom:1px solid #edf2ef;font-size:8px}.ufp-table th{background:#f7faf8;color:#617269}.ufp-table td strong{color:#087a45}.ufp-code{padding:18px;border-radius:17px;background:linear-gradient(135deg,#087a45,#075b38);color:#fff;display:flex;justify-content:space-between;align-items:center;gap:12px}.ufp-code small{display:block;color:rgba(255,255,255,.75);font-size:8px}.ufp-code strong{display:block;margin-top:7px;font-size:24px;letter-spacing:3px;direction:ltr;text-align:right}.ufp-list{display:grid;gap:8px}.ufp-item{padding:12px;border:1px solid #e2ebe6;border-radius:14px;display:grid;grid-template-columns:minmax(0,1fr) auto;gap:10px;align-items:center}.ufp-item h4{margin:0;font-size:10px}.ufp-item p{margin:5px 0 0;color:#76877e;font-size:8px;line-height:1.8}.ufp-empty,.ufp-loading,.ufp-error{padding:27px;text-align:center;border:1px dashed #cfdfd7;border-radius:16px;color:#687870;background:#fbfdfc;font-size:9px;line-height:1.9}.ufp-error{background:#fff5f6;color:#a72b3c}.ufp-note{padding:12px 14px;border-radius:13px;background:#fff8e9;color:#785d24;font-size:8px;line-height:1.9}.ufp-form{display:grid;grid-template-columns:1fr 1fr;gap:9px}.ufp-field{display:grid;gap:5px}.ufp-field span{font-size:7px;font-weight:900;color:#4c6257}.ufp-input{width:100%;border:1px solid #d9e5df;border-radius:10px;padding:9px;font:inherit;font-size:8px}.ufp-ledger{max-height:390px;overflow:auto}@media(max-width:1000px){.ufp-tabs{grid-template-columns:repeat(2,1fr)}.ufp-grid,.ufp-kpis{grid-template-columns:repeat(2,1fr)}}@media(max-width:650px){.ufp-tabs,.ufp-grid,.ufp-grid.two,.ufp-kpis,.ufp-form{grid-template-columns:1fr}.ufp-gauge-card{grid-template-columns:1fr;justify-items:center}.ufp-item{grid-template-columns:1fr}}
`;document.head.appendChild(style)}
function badgeClass(item){if(item?.eligible)return '';if(item?.status==='SCORE_BELOW_THRESHOLD')return 'danger';return 'warn'}
function gauge(title,progress,center,caption,item){return `<article class="ufp-gauge-card"><div class="ufp-gauge" style="--p:${pct(progress)}"><div><strong>${esc(center)}</strong><small>${fa(pct(progress))}٪</small></div></div><div class="ufp-gauge-copy"><h4>${esc(title)}</h4><p>${esc(caption)}</p>${item?`<span class="ufp-badge ${badgeClass(item)}">${esc(statusLabel[item.status]||item.status||'—')}</span>`:''}</div></article>`}
function evaluationKpis(data){const e=data.evaluation||{};return `<div class="ufp-kpis"><article class="ufp-kpi"><small>میانگین ارزیابی‌های نهایی</small><strong>${score(e.averageScore)}</strong></article><article class="ufp-kpi"><small>کمترین امتیاز نهایی</small><strong>${score(e.minimumScore)}</strong></article><article class="ufp-kpi"><small>آخرین امتیاز نهایی</small><strong>${score(e.latestScore)}</strong></article><article class="ufp-kpi"><small>تعداد ارزیابی FINAL</small><strong>${fa(e.finalizedPeriods)}</strong></article></div>`}
function requestButton(item,data){if(!item?.eligible)return '';const open=hasOpenRequest(data);return `<button type="button" class="ufp-btn primary" data-benefit-request="${esc(item.key)}" ${open?'disabled':''}>${open?'یک درخواست باز دارید':'ثبت درخواست این تسهیلات'}</button>`}
function retentionPanel(data){const a=data.allowance||{};const scorePct=metricProgress(a);return `<section class="ufp-panel ${state.activeTab==='retention'?'active':''}" data-panel="retention">${evaluationKpis(data)}<section class="ufp-card"><header class="ufp-card-head"><div><h3>کمک‌هزینه ماندگاری دوماهه</h3><p>دو ماه قرارداد ممتد و پایین نیامدن امتیاز نهایی ارزیابی از ۵۰.</p></div><strong>${money(a.amountToman||7000000)}</strong></header><div class="ufp-body"><div class="ufp-grid two">${gauge('پیشرفت سابقه ماندگاری',a.progressPercent,`${fa(a.progressPercent)}٪`,`سابقه محاسبه‌شده: ${duration(a.serviceDuration)} • ${fa(a.remainingDays)} روز باقی‌مانده`,a)}${gauge('اعتبارشمار ارزیابی',scorePct,score(a.evaluation?.metric),`کمترین امتیاز نهایی دوره • شرط: ${comparison(a)}`,a)}</div><div class="ufp-actions">${requestButton({...a,key:'ASSISTANCE_2M'},data)}</div></div></section><div class="ufp-note">امتیاز این بخش مستقیماً از ارزیابی‌های نهایی‌شده مراقب خوانده می‌شود؛ ارزیابی پیش‌نویس یا ناتمام در اعتبار محاسبه نمی‌شود.</div></section>`}
function loanCard(item,data){const ep=eligibilityProgress(item);return `<article class="ufp-card"><header class="ufp-card-head"><div><h3>${esc(item.title||'وام مراقبین')}</h3><p>${item.serviceMode==='CUMULATIVE'?'سابقه تجمیعی':'قرارداد ممتد'} • ${fa(item.targetMonths)} ماه • امتیاز ${comparison(item)}</p></div><strong>${money(item.amountToman)}</strong></header><div class="ufp-body">${gauge('اعتبارشمار این سطح',ep,`${fa(ep)}٪`,`سابقه: ${duration(item.serviceDuration)} • امتیاز مؤثر: ${score(item.evaluation?.metric)} • ${fa(item.remainingDays)} روز باقی‌مانده`,item)}<div class="ufp-actions">${requestButton(item,data)}</div></div></article>`}
function guideTable(){return `<section class="ufp-card"><header class="ufp-card-head"><div><h3>راهنمای نظام تسهیلات و اعتبارات مبتنی بر ارزیابی</h3><p>این جدول همان منطق مورد استفاده سامانه برای احراز اولیه است.</p></div></header><div class="ufp-guide"><table class="ufp-table"><thead><tr><th>تسهیلات</th><th>سابقه</th><th>شرط ارزیابی</th><th>مبلغ</th></tr></thead><tbody><tr><td>کمک‌هزینه ماندگاری</td><td>۲ ماه ممتد</td><td>کمترین امتیاز ≥ ۵۰</td><td><strong>۷,۰۰۰,۰۰۰ تومان</strong></td></tr><tr><td>وام سطح ۱</td><td>۳ ماه ممتد</td><td>میانگین ≥ ۵۰</td><td><strong>۷,۰۰۰,۰۰۰ تومان</strong></td></tr><tr><td>وام سطح ۲</td><td>۶ ماه ممتد</td><td>میانگین &gt; ۶۰</td><td><strong>۱۲,۰۰۰,۰۰۰ تومان</strong></td></tr><tr><td>وام سطح ۳</td><td>۱۲ ماه ممتد</td><td>میانگین &gt; ۶۵</td><td><strong>۵۰,۰۰۰,۰۰۰ تومان</strong></td></tr><tr><td>وام سطح ۴</td><td>۲۴ ماه ممتد</td><td>میانگین &gt; ۷۰</td><td><strong>۳۰۰,۰۰۰,۰۰۰ تومان</strong></td></tr><tr><td>وام سطح ۵</td><td>۷۰ ماه تجمیعی</td><td>میانگین ≥ ۶۵</td><td><strong>۵۰۰,۰۰۰,۰۰۰ تومان</strong></td></tr></tbody></table></div></section>`}
function loanPanel(data){return `<section class="ufp-panel ${state.activeTab==='loans'?'active':''}" data-panel="loans">${evaluationKpis(data)}<div class="ufp-grid two">${(data.loans||[]).map(x=>loanCard(x,data)).join('')}</div>${guideTable()}<div class="ufp-note">واجد شرایط بودن به معنی پرداخت خودکار وام نیست؛ درخواست پس از احراز سابقه و امتیاز وارد فرآیند بررسی و تأیید مالی می‌شود.</div></section>`}
function referralStatus(item){if(item.referrerConfirmationStatus==='PENDING')return 'منتظر تأیید شما';return statusLabel[item.status]||item.status||'—'}
function referralPanel(data){const r=data.referrals||{},s=r.summary||{},cases=r.cases||[],code=data.caregiver?.referralCode||'—';const total=Math.max(1,Number(s.totalReferrals||0));return `<section class="ufp-panel ${state.activeTab==='referrals'?'active':''}" data-panel="referrals"><section class="ufp-card"><div class="ufp-body"><div class="ufp-code"><div><small>کد معرفی یکتای شما</small><strong>${esc(code)}</strong></div><button type="button" class="ufp-btn" data-copy-code>کپی کد معرفی</button></div></div></section><div class="ufp-grid">${gauge('تأیید ثبت‌نام معرفی‌ها',Number(s.approvedRegistrations||0)/total*100,`${fa(s.approvedRegistrations)}/${fa(s.totalReferrals)}`,'تعداد ثبت‌نام‌هایی که پاداش مرحله اولشان قطعی شده است.')}${gauge('معرفی منجر به قرارداد',Number(s.completedReferrals||0)/total*100,`${fa(s.completedReferrals)}/${fa(s.totalReferrals)}`,'تعداد معرفی‌هایی که ورودشان به قرارداد تأیید شده است.')}${gauge('تا پاداش ۵ میلیونی',Number(s.progressInCycle||0)/10*100,`${fa(s.progressInCycle||0)}/۱۰`,`${fa(s.remainingToMilestone||10)} معرفی قراردادی دیگر تا پاداش بعدی`)}</div><section class="ufp-card"><header class="ufp-card-head"><div><h3>معرفی‌ها و اعتبارات معرفی</h3><p>پاداش قطعی: ${money(s.confirmedRewardToman)} • منتظر تأیید شما: ${fa(s.awaitingMyConfirmation)}</p></div></header><div class="ufp-body">${cases.length?`<div class="ufp-list">${cases.map(item=>`<article class="ufp-item"><div><h4>${esc(item.referredName||'مراقب معرفی‌شده')}</h4><p>${esc(item.referredMembershipCode||'—')} • ${esc(referralStatus(item))} • ${dateFa(item.createdAt)}</p></div><div>${item.referrerConfirmationStatus==='PENDING'?`<div class="ufp-actions"><button class="ufp-btn danger" data-ref-decision="REJECT" data-ref-id="${esc(item.id)}">رد</button><button class="ufp-btn primary" data-ref-decision="CONFIRM" data-ref-id="${esc(item.id)}">تأیید</button></div>`:`<span class="ufp-badge">${money((item.registrationRewardTransactionId?Number(item.registrationRewardToman||0):0)+(item.contractRewardTransactionId?Number(item.contractRewardToman||0):0))}</span>`}</div></article>`).join('')}</div>`:'<div class="ufp-empty">هنوز معرفی ثبت‌شده‌ای ندارید.</div>'}</div></section></section>`}
function walletPanel(data){const w=data.wallet||{},s=w.summary||{},tx=w.transactions||[],alloc=w.allocations||[],settlements=w.settlements||[],requests=w.creditRequests||[];const total=Math.max(1,Number(s.creditToman||0)+Number(s.debitToman||0));return `<section class="ufp-panel ${state.activeTab==='wallet'?'active':''}" data-panel="wallet"><div class="ufp-grid">${gauge('مانده خالص کیف پول',Number(s.balanceToman)>0?100:0,money(s.balanceToman),'مانده فعلی ثبت‌شده در دفتر مالی')}${gauge('بستانکاری ثبت‌شده',Number(s.creditToman)/total*100,money(s.creditToman),'جمع تمام ورودی‌های بستانکار دفتر کیف پول')}${gauge('بدهکاری / برداشت ثبت‌شده',Number(s.debitToman)/total*100,money(s.debitToman),'جمع تمام خروجی‌ها و برداشت‌های ثبت‌شده')}</div><div class="ufp-kpis"><article class="ufp-kpi"><small>قابل تسویه</small><strong>${money(s.availableToman)}</strong></article><article class="ufp-kpi"><small>تسویه در جریان</small><strong>${money(s.pendingSettlementToman)}</strong></article><article class="ufp-kpi"><small>تعداد تخصیص‌ها</small><strong>${fa(alloc.length)}</strong></article><article class="ufp-kpi"><small>درخواست تسهیلات</small><strong>${fa(requests.length)}</strong></article></div><section class="ufp-card"><header class="ufp-card-head"><div><h3>اقدامات مالی</h3><p>ثبت درخواست تسویه از مانده قابل تسویه کیف پول.</p></div></header><div class="ufp-body"><form class="ufp-form" data-settlement-form><label class="ufp-field"><span>مبلغ تسویه (تومان)</span><input class="ufp-input" name="amountToman" inputmode="numeric" required></label><label class="ufp-field"><span>نام صاحب حساب</span><input class="ufp-input" name="accountHolderName" required></label><label class="ufp-field"><span>شماره شبا</span><input class="ufp-input" name="iban" placeholder="IR..." dir="ltr"></label><label class="ufp-field"><span>شماره حساب</span><input class="ufp-input" name="accountNumber" dir="ltr"></label><label class="ufp-field"><span>بانک</span><input class="ufp-input" name="bankName"></label><label class="ufp-field"><span>یادداشت</span><input class="ufp-input" name="note"></label><div class="ufp-actions"><button class="ufp-btn primary" type="submit">ثبت درخواست تسویه</button></div></form></div></section><section class="ufp-card"><header class="ufp-card-head"><div><h3>لیست تخصیص اعتبارات</h3><p>اعتبارات، پاداش‌ها و درخواست‌های تسهیلات از دفتر واحد مالی.</p></div></header><div class="ufp-guide">${alloc.length?`<table class="ufp-table"><thead><tr><th>عنوان</th><th>نوع</th><th>مبلغ</th><th>وضعیت</th><th>تاریخ</th></tr></thead><tbody>${alloc.map(x=>`<tr><td>${esc(x.title)}</td><td>${esc(x.kind)}</td><td><strong>${money(x.amountToman)}</strong></td><td>${esc(statusLabel[x.status]||x.status)}</td><td>${dateFa(x.createdAt)}</td></tr>`).join('')}</tbody></table>`:'<div class="ufp-empty">تخصیص اعتباری ثبت نشده است.</div>'}</div></section><section class="ufp-card"><header class="ufp-card-head"><div><h3>دفتر کیف پول</h3><p>تمام بستانکاری‌ها و بدهکاری‌ها به ترتیب زمان.</p></div></header><div class="ufp-guide ufp-ledger">${tx.length?`<table class="ufp-table"><thead><tr><th>شرح</th><th>جهت</th><th>مبلغ</th><th>تاریخ</th></tr></thead><tbody>${tx.map(x=>`<tr><td>${esc(x.title)}<br><small>${esc(x.description||'')}</small></td><td>${x.direction==='CREDIT'?'بستانکار':'بدهکار / برداشت'}</td><td><strong>${money(x.amountToman)}</strong></td><td>${dateFa(x.createdAt)}</td></tr>`).join('')}</tbody></table>`:'<div class="ufp-empty">هنوز تراکنشی ثبت نشده است.</div>'}</div></section></section>`}
function markup(data){return `<section class="ufp" id="caregiverUnifiedFinancialV4" data-version="${VERSION}"><header class="ufp-head"><div><h2>کیف پول و اعتبارات</h2><p>کارنامه یکپارچه مالی شما؛ ارزیابی‌های نهایی، سابقه قرارداد، معرفی‌ها و دفتر کیف پول همگی از یک جریان داده محاسبه می‌شوند.</p></div><span class="ufp-badge">نسخه یکپارچه ${VERSION}</span></header><nav class="ufp-tabs" aria-label="بخش‌های کیف پول"><button class="ufp-tab ${state.activeTab==='retention'?'active':''}" data-tab="retention">کمک‌هزینه ماندگاری دوماهه</button><button class="ufp-tab ${state.activeTab==='loans'?'active':''}" data-tab="loans">پلکان وام مراقبین</button><button class="ufp-tab ${state.activeTab==='referrals'?'active':''}" data-tab="referrals">معرفی و اعتبارات معرفی مراقب</button><button class="ufp-tab ${state.activeTab==='wallet'?'active':''}" data-tab="wallet">کیف پول و اقدامات مالی</button></nav>${retentionPanel(data)}${loanPanel(data)}${referralPanel(data)}${walletPanel(data)}</section>`}
async function load(force=false){if(state.data&&!force&&Date.now()-state.lastLoadedAt<30000)return state.data;if(state.promise)return state.promise;state.promise=api(ENDPOINT).then(p=>{state.data=p.data||{};state.lastLoadedAt=Date.now();return state.data}).finally(()=>state.promise=null);return state.promise}
function host(){return $('#content .module-page')||$('#content')}
function bind(root){$$('[data-tab]',root).forEach(btn=>btn.addEventListener('click',()=>{state.activeTab=btn.dataset.tab;$$('[data-tab]',root).forEach(x=>x.classList.toggle('active',x===btn));$$('[data-panel]',root).forEach(x=>x.classList.toggle('active',x.dataset.panel===state.activeTab))}));$('[data-copy-code]',root)?.addEventListener('click',async()=>{const code=state.data?.caregiver?.referralCode||'';try{await navigator.clipboard.writeText(code);toast('کد معرفی کپی شد',code)}catch{window.prompt('کد معرفی شما',code)}});$$('[data-benefit-request]',root).forEach(btn=>btn.addEventListener('click',()=>requestBenefit(btn.dataset.benefitRequest)));$$('[data-ref-decision]',root).forEach(btn=>btn.addEventListener('click',()=>refDecision(btn.dataset.refId,btn.dataset.refDecision)));$('[data-settlement-form]',root)?.addEventListener('submit',submitSettlement)}
async function requestBenefit(key){if(state.busy)return;if(!confirm('درخواست این تسهیلات برای بررسی مالی ثبت شود؟'))return;state.busy=true;try{await api('/api/caregiver/platform/credit-requests',{method:'POST',body:JSON.stringify({benefitKey:key})});toast('درخواست ثبت شد','پرونده برای بررسی مالی ارسال شد.');state.data=null;await render(true)}catch(e){toast('ثبت درخواست انجام نشد',e.message)}finally{state.busy=false}}
async function refDecision(id,action){if(state.busy)return;let note='';if(action==='REJECT'){note=prompt('علت رد معرفی را وارد کنید:','')||'';if(!note.trim())return}else if(!confirm('این فرد واقعاً با کد معرفی شما ثبت‌نام کرده است؟'))return;state.busy=true;try{await api(`/api/caregiver/platform/referrals/${encodeURIComponent(id)}`,{method:'PATCH',body:JSON.stringify({action,note})});toast('وضعیت معرفی ثبت شد','اطلاعات به‌روزرسانی شد.');state.data=null;await render(true)}catch(e){toast('ثبت تصمیم انجام نشد',e.message)}finally{state.busy=false}}
async function submitSettlement(event){event.preventDefault();if(state.busy)return;const form=event.currentTarget,fd=new FormData(form),body=Object.fromEntries(fd.entries());body.amountToman=String(body.amountToman||'').replace(/\D/g,'');state.busy=true;try{await api('/api/caregiver/platform/settlements',{method:'POST',body:JSON.stringify(body)});toast('درخواست تسویه ثبت شد','درخواست برای بررسی مالی ارسال شد.');state.data=null;await render(true)}catch(e){toast('ثبت تسویه انجام نشد',e.message)}finally{state.busy=false}}
async function render(force=false){if(!isWallet()){$('#caregiverUnifiedFinancialV4')?.remove();return}const target=host();if(!target)return;if(!$('#caregiverUnifiedFinancialV4',target)){target.innerHTML='<section class="ufp" id="caregiverUnifiedFinancialV4"><div class="ufp-loading">در حال ساخت کارنامه یکپارچه مالی…</div></section>'}try{const data=await load(force);if(!isWallet())return;target.innerHTML=markup(data);bind($('#caregiverUnifiedFinancialV4',target))}catch(e){target.innerHTML=`<section class="ufp"><div class="ufp-error">${esc(e.message||'دریافت کارنامه مالی انجام نشد.')}</div></section>`}}
function schedule(){clearTimeout(state.timer);state.timer=setTimeout(()=>render(false),80)}
addStyles();schedule();['#content','#sidebarNav'].forEach(selector=>{const node=$(selector);if(!node)return;const observer=new MutationObserver(schedule);observer.observe(node,{childList:true,subtree:true,attributes:true,attributeFilter:['class','data-caregiver-module-key']});state.observers.push(observer)});window.SalamatUnifiedFinancialV4={version:VERSION,refresh:()=>{state.data=null;return render(true)}};
})();
