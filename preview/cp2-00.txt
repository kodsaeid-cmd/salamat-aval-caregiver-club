(()=>{
const K='salamatAvalCaregiverPanelV1',q=s=>document.querySelector(s),qa=s=>[...document.querySelectorAll(s)],fa=n=>Number(n||0).toLocaleString('fa-IR'),tm=n=>`${fa(n)} تومان`;
let s={};try{s=JSON.parse(localStorage.getItem(K))||{}}catch{}
s.profile=s.profile||{name:'مریم حسینی',code:'SA-1405-1028',supportAgent:'نگار احمدی'};s.wallet=s.wallet||{balance:0,withdrawable:0,pending:0,transactions:[],payoutRequests:[]};s.payslips=s.payslips||[];s.courses=s.courses||[];
const contracts=s.contracts||[{id:'CT-1405-0412',family:'خانواده مرادی',service:'مراقبت سالمند',start:'۱ تیر ۱۴۰۵',end:'۳۱ شهریور ۱۴۰۵',status:'فعال',type:'ساعتی',hours:168,logged:144,overtime:8,absent:0,days:[1,2,3,4,5,8,9,10,11,12,15,16,17,18,19,22,23,24,25,26,29,30,31]},{id:'CT-1405-0106',family:'خانواده علوی',service:'همراهی سالمند',start:'۱ فروردین ۱۴۰۵',end:'۳۱ خرداد ۱۴۰۵',status:'پایان‌یافته',type:'ماهانه',hours:192,logged:190,overtime:4,absent:2,days:[]}];
const opNames=['هویت حرفه‌ای','پایبندی به استانداردهای مراقبتی','استانداردهای رفتاری','حفظ کرامت سالمند','ارتباط با خانواده','استانداردهای ظاهری','هم‌راستایی فرهنگی'],trustNames=['اعتماد خانواده','عملکرد حرفه‌ای','قابلیت اتکا','کرامت و احترام','رشد و توسعه حرفه‌ای','هم‌راستایی با برند'];
const evals=JSON.parse(localStorage.getItem('salamatAvalEvaluationV1')||'{"operational":[],"trust":[],"history":[],"warnings":[]}'),sum=a=>a.length?a.reduce((x,y)=>x+Number(y.score||0),0):null,op=()=>sum(evals.operational),tr=()=>sum(evals.trust),band=x=>x==null?['بدون ارزیابی','پس از ثبت ارزیابی رسمی محاسبه می‌شود']:x>=55?['Elite Caregiver','مناسب پرونده‌های VIP و حساس']:x>=40?['Professional Caregiver','مناسب اکثر سرویس‌لاین‌ها']:x>=20?['Conditional Approval','نیازمند Coaching و بازآموزی']:['Performance Improvement Required','ورود به برنامه اصلاح عملکرد'];
const oldD=renderDashboard,oldM=renderModule;let ni=roles.caregiver.nav.find(x=>x[1]==='پشتیبانی پرونده');if(ni)ni[1]='پشتیبانی قراردادها';
const empty=(i,t,d)=>`<div class="cp-empty"><span data-icon="${i}"></span><strong>${t}</strong><small>${d}</small></div>`,prog=v=>`<div class="cp-progress"><span class="green" style="width:${Math.max(0,Math.min(100,v||0))}%"></span></div>`;
function set(title,html){q('#pageTitle').textContent=title;q('#pageSubtitle').textContent=`پنل مراقب • ${s.profile.code}`;q('#content').innerHTML=`<section class="module-page caregiver-module">${html}</section>`;hydrateIcons(q('#content'));bind()}
function dash(){let c=contracts.find(x=>x.status==='فعال'),p=s.payslips[0],o=op(),t=tr();q('#pageTitle').textContent='داشبورد مراقب';q('#pageSubtitle').textContent='قرارداد، پرداخت، آموزش و ارزیابی حرفه‌ای';q('#content').innerHTML=`<section class="role-hero caregiver-hero-panel"><div><span class="cp-eyebrow">باشگاه مراقبین سلامت اول</span><h2>سلام ${s.profile.name.split(' ')[0]}، پرونده کاری شما آماده بررسی است</h2><p>تمام اطلاعات این پنل از قرارداد، حقوق، بانک آموزش و نظام پایش تغذیه می‌شود.</p></div><div class="hero-score"><div class="score-ring"><strong>${o==null?'—':fa(o)}</strong></div><div><span>ارزیابی عملیاتی از ۷۰</span><small>${o==null?'داده رسمی ثبت نشده':'محاسبه‌شده از ارزیابی'}</small></div></div></section><section class="cp2-kpis"><article class="cp2-kpi"><small>قرارداد جاری</small><strong>${c?.id||'—'}</strong><span>${c?c.start+' تا '+c.end:'بدون قرارداد'}</span></article><article class="cp2-kpi"><small>فیش ماه جاری</small><strong>${p?tm(p.net):'—'}</strong><span>${p?.month||'صادر نشده'}</span></article><article class="cp2-kpi"><small>Trust Score</small><strong>${t==null?'—':fa(t)+' / ۶۰'}</strong><span>${t==null?'داده CRM ثبت نشده':'شش شاخص اعتماد'}</span></article><article class="cp2-kpi"><small>آموزش‌های تخصیص‌یافته</small><strong>${fa(s.courses.length)}</strong><span>از بانک آموزش سازمان</span></article></section>${oldD?'<section class="surface"><div class="surface-head"><div><h3>میانبرها</h3><p>از منوی کنار وارد هر ماژول شوید</p></div></div></section>':''}`;hydrateIcons(q('#content'))}
function perf(){let o=op(),t=tr(),h=evals.history||[];return `<section class="cp-page-head"><div><span class="cp-eyebrow">Business Intelligence</span><h2>داشبورد BI کارنامه کاری</h2><p>هیچ عددی خارج از رکوردهای ثبت‌شده در نظام پایش، CRM، حضور و آموزش نمایش داده نمی‌شود.</p></div><div class="cp2-filterbar"><select>${contracts.map(c=>`<option>${c.id} • ${c.family}</option>`).join('')}</select><select><option>ماه جاری</option><option>سه‌ماهه</option><option>شش‌ماهه</option><option>کل همکاری</option></select></div></section><section class="cp2-kpis"><article class="cp2-kpi"><small>امتیاز عملیاتی</small><strong>${o==null?'—':fa(o)+' / ۷۰'}</strong><span>${band(o)[0]}</span></article><article class="cp2-kpi"><small>Trust Score</small><strong>${t==null?'—':fa(t)+' / ۶۰'}</strong><span>ارزیابی CRM</span></article><article class="cp2-kpi"><small>هشدار باز</small><strong>${fa((evals.warnings||[]).length)}</strong><span>نیازمند بازبینی</span></article><article class="cp2-kpi"><small>دوره‌های ارزیابی</small><strong>${h.length?fa(h.length):'—'}</strong><span>رکورد رسمی</span></article></section><section class="cp2-bi-grid"><article class="surface cp2-chart"><h3>روند دوره‌ای عملکرد</h3><p>نمودار پس از ثبت حداقل یک دوره رسمی فعال می‌شود.</p>${h.length?`<div class="cp2-bars">${h.slice(-6).map(x=>`<div class="cp2-bar-col"><strong>${fa(x.score)}</strong><div class="cp2-bar" style="height:${Math.round(x.score/70*170)}px"></div><small>${x.period}</small></div>`).join('')}</div>`:empty('chart','داده روند وجود ندارد','اولین ارزیابی رسمی باید از پنل پایش ثبت شود.')}</article><article class="surface"><div class="surface-head"><div><h3>هفت شاخص عملیاتی</h3><p>هر شاخص حداکثر ۱۰ امتیاز</p></div></div><div class="cp2-score-grid">${opNames.map(n=>{let r=evals.operational.find(x=>x.title===n);return `<div class="cp2-score-card"><header><strong>${n}</strong><b>${r?fa(r.score):'—'}</b></header>${prog(r?r.score*10:0)}<small>${r?'ثبت‌شده توسط سیستم اجرایی':'ارزیابی ثبت نشده'}</small></div>`}).join('')}</div></article></section><section class="surface" style="margin-top:14px"><div class="surface-head"><div><h3>شش شاخص Trust Score</h3><p>ارزیابی اعتماد توسط CRM</p></div></div><div class="cp2-score-grid">${trustNames.map(n=>{let r=evals.trust.find(x=>x.title===n);return `<div class="cp2-score-card"><header><strong>${n}</strong><b>${r?fa(r.score):'—'}</b></header>${prog(r?r.score*10:0)}<small>${r?'ثبت‌شده در CRM':'داده‌ای ثبت نشده'}</small></div>`}).join('')}</div></section>`}
function rank(){let o=op(),b=band(o);return `<section class="cp2-rank-hero"><div><span class="cp-eyebrow">نظام ارزیابی و پایش</span><h2>${b[0]}</h2><p>${b[1]}. رتبه بر پایه کیفیت پایدار، رفتار حرفه‌ای و 