(()=>{
'use strict';
if(window.__salamatStaffSupportRuntimeV1)return;
window.__salamatStaffSupportRuntimeV1=true;

const VERSION='1.0.0';
const $=(selector,root=document)=>root.querySelector(selector);
const esc=value=>String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));
const dateFa=value=>value?new Intl.DateTimeFormat('fa-IR-u-ca-persian',{year:'numeric',month:'short',day:'numeric',hour:'2-digit',minute:'2-digit'}).format(new Date(value)):'—';
const state={baseRender:null,threads:[],activeId:'',messages:[],filter:'ALL',recording:null,chunks:[]};

async function api(path,options={}){const headers=new Headers(options.headers||{});if(typeof options.body==='string'&&!headers.has('content-type'))headers.set('content-type','application/json');const response=await fetch(path,{credentials:'same-origin',cache:'no-store',...options,headers});const text=await response.text();let payload={};try{payload=text?JSON.parse(text):{}}catch{payload={detail:text}}if(!response.ok){const error=new Error(payload.message||`خطای ${response.status}`);error.status=response.status;error.code=payload.error;throw error}return payload}
function currentUser(){try{return window.SalamatBackend?.getCurrentUser?.()||null}catch{return null}}
function notify(title,text){try{window.toast?.(title,text)}catch{}if(!window.toast)console.info(title,text)}
function status(value){return ({OPEN:'باز',PENDING:'در انتظار پاسخ',RESOLVED:'حل‌شده',CLOSED:'بسته'}[String(value||'').toUpperCase()]||value||'—')}
function setPage(html){const title=$('#pageTitle'),subtitle=$('#pageSubtitle'),content=$('#content');if(title)title.textContent='پشتیبانی';if(subtitle)subtitle.textContent='پشتیبانی پرونده و صف فوری و امنیتی مراقبین';if(content)content.innerHTML=`<section class="module-page sts-root">${html}</section>`}
function loading(){return '<div class="sts-empty"><span class="sts-spinner"></span><strong>در حال دریافت صف پشتیبانی...</strong></div>'}
function empty(title,text){return `<div class="sts-empty"><strong>${esc(title)}</strong><small>${esc(text)}</small></div>`}
function addStyles(){if($('#staffSupportStylesV1'))return;const style=document.createElement('style');style.id='staffSupportStylesV1';style.textContent=`
.sts-root{direction:rtl;display:grid;gap:12px}.sts-head{display:flex;align-items:center;justify-content:space-between;gap:10px;flex-wrap:wrap}.sts-head h2{margin:0;font-size:20px}.sts-head p{margin:6px 0 0;color:#78877f;font-size:9px}.sts-filters{display:flex;gap:7px;flex-wrap:wrap}.sts-btn{border:0;border-radius:11px;padding:10px 13px;background:#edf8f2;color:#087747;font:inherit;font-size:8px;font-weight:900;cursor:pointer}.sts-btn.active,.sts-btn.primary{background:#078848;color:#fff}.sts-btn.danger{background:#ffe8ec;color:#ae2940}.sts-btn:disabled{opacity:.45;pointer-events:none}.sts-layout{display:grid;grid-template-columns:minmax(280px,.7fr) minmax(0,1.3fr);gap:12px;align-items:stretch}.sts-card{background:#fff;border:1px solid #dce8e2;border-radius:20px;overflow:hidden;box-shadow:0 10px 28px rgba(20,70,45,.04)}.sts-card-head{padding:14px 16px;border-bottom:1px solid #eaf0ed;display:flex;align-items:center;justify-content:space-between;gap:10px}.sts-card-head h3{margin:0;font-size:13px}.sts-card-head p{margin:5px 0 0;color:#7b8982;font-size:8px}.sts-queue{padding:12px;display:grid;gap:7px;max-height:650px;overflow:auto}.sts-thread{width:100%;border:1px solid #e0eae5;border-radius:14px;background:#fff;padding:11px;text-align:right;cursor:pointer}.sts-thread.active{border-color:#0a9253;background:#f0faf5}.sts-thread.urgent{border-color:#f0c9d1;background:#fff8f9}.sts-thread header{display:flex;justify-content:space-between;gap:8px}.sts-thread strong{font-size:9px}.sts-thread small{display:block;margin-top:5px;color:#7b8982;font-size:7px;line-height:1.75}.sts-badge{display:inline-flex;padding:5px 8px;border-radius:999px;background:#edf8f2;color:#087747;font-size:7px;font-weight:900}.sts-badge.urgent{background:#ffe8ec;color:#ad2940}.sts-chat{display:grid;grid-template-rows:auto minmax(300px,1fr) auto;min-height:650px}.sts-messages{padding:14px;background:#f7faf8;overflow:auto;display:grid;align-content:start;gap:8px}.sts-message{max-width:78%;justify-self:start;padding:10px 12px;border:1px solid #e0e9e4;border-radius:15px 15px 15px 4px;background:#fff}.sts-message.mine{justify-self:end;background:#e8f7ef;border-color:#cce9d9;border-radius:15px 15px 4px 15px}.sts-message p{margin:0;font-size:9px;line-height:1.9}.sts-message small{display:block;margin-top:5px;color:#829087;font-size:7px}.sts-message audio{width:min(280px,100%);height:36px}.sts-compose{padding:10px;border-top:1px solid #e7efeb;display:grid;grid-template-columns:auto minmax(0,1fr) auto;gap:7px}.sts-input,.sts-select{width:100%;box-sizing:border-box;border:1px solid #d8e4de;border-radius:11px;padding:10px;background:#fff;font:inherit;font-size:8px}.sts-recording{background:#ffe8ec!important;color:#ae2940!important}.sts-empty{min-height:200px;display:grid;place-items:center;align-content:center;gap:8px;text-align:center;color:#65776d}.sts-empty strong{font-size:10px}.sts-empty small{font-size:8px;line-height:1.8;max-width:430px}.sts-spinner{width:26px;height:26px;border:3px solid #dcece4;border-top-color:#078848;border-radius:50%;animation:stsSpin .8s linear infinite}@keyframes stsSpin{to{transform:rotate(360deg)}}@media(max-width:950px){.sts-layout{grid-template-columns:1fr}.sts-chat{min-height:560px}}@media(max-width:600px){.sts-compose{grid-template-columns:auto minmax(0,1fr)}.sts-compose .send{grid-column:1/-1}.sts-message{max-width:90%}}
`;(document.head||document.documentElement).appendChild(style)}

function filtered(){return state.threads.filter(thread=>state.filter==='ALL'||thread.category===state.filter)}
function queueMarkup(){const rows=filtered();return rows.length?rows.map(thread=>`<button class="sts-thread ${thread.id===state.activeId?'active':''} ${thread.category==='URGENT_SECURITY'?'urgent':''}" data-sts-thread="${esc(thread.id)}"><header><strong>${esc(thread.caregiverName)}</strong><span class="sts-badge ${thread.category==='URGENT_SECURITY'?'urgent':''}">${thread.category==='URGENT_SECURITY'?'فوری':'پرونده'}</span></header><small>${esc(thread.subject)}<br>${esc(thread.membershipCode||'')} • ${esc(thread.familyName||thread.contractNumber||'بدون قرارداد')}<br>${status(thread.status)} • ${dateFa(thread.updatedAt)}</small></button>`).join(''):empty('صف خالی است','گفت‌وگویی با این فیلتر وجود ندارد.')}
function chatMarkup(){const thread=state.threads.find(item=>item.id===state.activeId),user=currentUser();if(!thread)return empty('گفت‌وگویی انتخاب نشده','از صف سمت راست یک گفت‌وگو را انتخاب کنید.');return `<header class="sts-card-head"><div><h3>${esc(thread.caregiverName)} • ${esc(thread.subject)}</h3><p>${thread.category==='URGENT_SECURITY'?'پشتیبانی فوری و امنیتی':'پشتیبانی پرونده'} • ${esc(thread.familyName||thread.contractNumber||'بدون قرارداد')}</p></div><select class="sts-select" style="width:auto" data-sts-status><option value="OPEN" ${thread.status==='OPEN'?'selected':''}>باز</option><option value="PENDING" ${thread.status==='PENDING'?'selected':''}>در انتظار</option><option value="RESOLVED" ${thread.status==='RESOLVED'?'selected':''}>حل‌شده</option><option value="CLOSED" ${thread.status==='CLOSED'?'selected':''}>بسته</option></select></header><div class="sts-messages" id="stsMessages">${state.messages.length?state.messages.map(message=>`<div class="sts-message ${message.senderUserId===user?.id?'mine':''}">${message.messageType==='VOICE'?`<audio controls preload="none" src="/api/files/${encodeURIComponent(message.storedFileId)}/download"></audio>`:`<p>${esc(message.textContent)}</p>`}<small>${esc(message.senderName||'کاربر')} • ${dateFa(message.createdAt)}</small></div>`).join(''):empty('هنوز پیامی نیست','پاسخ متنی یا صوتی خود را ارسال کنید.')}</div><form class="sts-compose" id="stsMessageForm"><button class="sts-btn" type="button" data-sts-record>● وویس</button><input class="sts-input" name="text" placeholder="پاسخ خود را بنویسید..."><button class="sts-btn primary send">ارسال</button></form>`}
function shell(){const urgent=state.threads.filter(x=>x.category==='URGENT_SECURITY'&&['OPEN','PENDING'].includes(x.status)).length;return `<header class="sts-head"><div><h2>پشتیبانی مراقبین</h2><p>گفت‌وگوهای پرونده و درخواست‌های فوری و امنیتی در یک صف یکپارچه</p></div><nav class="sts-filters"><button class="sts-btn ${state.filter==='ALL'?'active':''}" data-sts-filter="ALL">همه</button><button class="sts-btn ${state.filter==='CASE'?'active':''}" data-sts-filter="CASE">پشتیبانی پرونده</button><button class="sts-btn ${state.filter==='URGENT_SECURITY'?'active':''}" data-sts-filter="URGENT_SECURITY">فوری و امنیتی (${urgent.toLocaleString('fa-IR')})</button></nav></header><section class="sts-layout"><aside class="sts-card"><header class="sts-card-head"><div><h3>صف گفت‌وگوها</h3><p>اولویت بحرانی در بالای صف قرار می‌گیرد.</p></div></header><div class="sts-queue">${queueMarkup()}</div></aside><article class="sts-card sts-chat">${chatMarkup()}</article></section>`}
function render(){setPage(shell());requestAnimationFrame(()=>{const box=$('#stsMessages');if(box)box.scrollTop=box.scrollHeight})}
async function load(openFirst=true){setPage(loading());try{const payload=await api('/api/caregiver/platform/support/threads');state.threads=payload.data.threads||[];if(openFirst&&!state.activeId&&state.threads[0])state.activeId=state.threads[0].id;if(state.activeId&&!state.threads.some(x=>x.id===state.activeId))state.activeId='';if(state.activeId)await loadMessages(false);render()}catch(error){setPage(empty('پشتیبانی بارگذاری نشد',error.message))}}
async function loadMessages(renderAfter=true){if(!state.activeId)return;const payload=await api(`/api/caregiver/platform/support/threads/${encodeURIComponent(state.activeId)}/messages`);state.messages=payload.data.messages||[];if(renderAfter)render()}
async function uploadVoice(blob,caregiverId){const form=new FormData(),extension=blob.type.includes('ogg')?'ogg':'webm';form.append('file',new File([blob],`staff-support-${Date.now()}.${extension}`,{type:blob.type||'audio/webm'}));form.append('category','support');form.append('caregiverId',caregiverId);const response=await fetch('/api/files',{method:'POST',body:form,credentials:'same-origin',cache:'no-store'});const payload=await response.json().catch(()=>({}));if(!response.ok)throw new Error(payload.message||'بارگذاری وویس انجام نشد.');return payload.data.id}
async function toggleRecording(button){if(state.recording){state.recording.stop();button.disabled=true;return}const thread=state.threads.find(item=>item.id===state.activeId);if(!thread)return;if(!navigator.mediaDevices?.getUserMedia||!window.MediaRecorder){notify('ضبط صدا پشتیبانی نمی‌شود','مرورگر یا دسترسی میکروفون آماده نیست.');return}try{const stream=await navigator.mediaDevices.getUserMedia({audio:true});const type=MediaRecorder.isTypeSupported('audio/webm;codecs=opus')?'audio/webm;codecs=opus':'audio/webm';state.chunks=[];state.recording=new MediaRecorder(stream,{mimeType:type});state.recording.ondataavailable=event=>{if(event.data.size)state.chunks.push(event.data)};state.recording.onstop=async()=>{const recorder=state.recording;state.recording=null;stream.getTracks().forEach(track=>track.stop());button.classList.remove('sts-recording');button.textContent='● وویس';try{const fileId=await uploadVoice(new Blob(state.chunks,{type:recorder?.mimeType||'audio/webm'}),thread.caregiverId);await api(`/api/caregiver/platform/support/threads/${encodeURIComponent(state.activeId)}/messages`,{method:'POST',body:JSON.stringify({storedFileId:fileId})});notify('وویس ارسال شد','پیام صوتی در گفت‌وگو ثبت شد.');await loadMessages(true)}catch(error){notify('ارسال وویس انجام نشد',error.message)}finally{button.disabled=false}};state.recording.start();button.classList.add('sts-recording');button.textContent='■ پایان ضبط'}catch(error){notify('دسترسی میکروفون داده نشد',error.message||'مجوز میکروفون را فعال کنید.')}}

function click(event){const filter=event.target?.closest?.('[data-sts-filter]');if(filter){event.preventDefault();state.filter=filter.dataset.stsFilter;const rows=filtered();if(state.activeId&&!rows.some(x=>x.id===state.activeId)){state.activeId=rows[0]?.id||'';state.messages=[];if(state.activeId)void loadMessages(true);else render()}else render();return}const thread=event.target?.closest?.('[data-sts-thread]');if(thread){event.preventDefault();state.activeId=thread.dataset.stsThread;void loadMessages(true);return}const record=event.target?.closest?.('[data-sts-record]');if(record){event.preventDefault();void toggleRecording(record)}}
async function submit(event){if(event.target?.id!=='stsMessageForm')return;event.preventDefault();const input=$('[name="text"]',event.target),text=String(input?.value||'').trim();if(!text)return;try{await api(`/api/caregiver/platform/support/threads/${encodeURIComponent(state.activeId)}/messages`,{method:'POST',body:JSON.stringify({text})});if(input)input.value='';await loadMessages(true)}catch(error){notify('ارسال پیام انجام نشد',error.message)}}
async function change(event){if(!event.target?.matches?.('[data-sts-status]')||!state.activeId)return;try{await api(`/api/caregiver/platform/support/threads/${encodeURIComponent(state.activeId)}`,{method:'PATCH',body:JSON.stringify({status:event.target.value})});notify('وضعیت به‌روزرسانی شد','گفت‌وگو در صف جدید قرار گرفت.');await load(false)}catch(error){notify('تغییر وضعیت انجام نشد',error.message)}}
function install(){const current=window.renderModule;if(current?.__staffSupportV1)return;if(!state.baseRender||!state.baseRender.__staffSupportV1)state.baseRender=current?.__staffSupportV1?current.__base:current;const wrapper=function(model,item){const label=String(item?.[1]||'');if(label==='پشتیبانی'||label==='پشتیبانی و امنیت')return load();return state.baseRender?.(model,item)};wrapper.__staffSupportV1=true;wrapper.__base=state.baseRender;window.renderModule=wrapper;try{renderModule=wrapper}catch{}}
function boot(){addStyles();document.addEventListener('click',click,true);document.addEventListener('submit',submit,true);document.addEventListener('change',change,true);install();let count=0;const timer=setInterval(()=>{count+=1;install();if(count>=150)clearInterval(timer)},100);window.SalamatStaffSupport={version:VERSION,open:load,reload:load}}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();
