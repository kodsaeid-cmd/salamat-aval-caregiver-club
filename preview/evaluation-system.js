(()=>{
'use strict';

const STORAGE_KEY='salamatAvalEvaluationSystemV13';
const UI_KEY='salamatAvalEvaluationUIV13';
const POLICY_VERSION='SAB-BB-1405-V1.3';
const $=(s,r=document)=>r.querySelector(s);
const $$=(s,r=document)=>[...r.querySelectorAll(s)];
const fa=n=>Number(n||0).toLocaleString('fa-IR');
const esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
const nowFa=()=>new Intl.DateTimeFormat('fa-IR',{dateStyle:'short',timeStyle:'short'}).format(new Date());
const uid=p=>`${p}-${Date.now().toString(36).toUpperCase()}-${Math.random().toString(36).slice(2,6).toUpperCase()}`;

const Q=[
 {code:'Q-01',title:'کیفیت ارائه خدمات',sources:'گزارش سوپروایزر، عملیات و نظارت',criteria:['انجام کامل شرح وظایف','رعایت برنامه مراقبتی','دقت در انجام امور','گزارش‌دهی صحیح','پیشگیری از خطاهای مراقبتی']},
 {code:'Q-02',title:'رضایت خدمت‌گیرنده و خانواده',sources:'نظرسنجی خانواده، CRM، شکایات و تقدیرها',criteria:['امتیاز نظرسنجی','تمدید قرارداد','تقدیر ثبت‌شده','عدم وجود شکایت مؤثر']},
 {code:'Q-03',title:'رعایت کرامت و حقوق خدمت‌گیرنده',sources:'گزارش نظارت، بازدید میدانی و شکایات',criteria:['حفظ احترام و شأن فرد','رعایت حریم خصوصی','رعایت محرمانگی اطلاعات','رعایت استقلال فرد در تصمیم‌گیری','پرهیز از رفتار تحقیرآمیز یا تبعیض‌آمیز']},
 {code:'Q-04',title:'اخلاق و رفتار حرفه‌ای',sources:'گزارش عملیات، سوپروایزر و واحد ارزیابی',criteria:['صداقت','احترام','مسئولیت‌پذیری','رعایت پوشش سازمانی','ارتباط حرفه‌ای با خانواده و همکاران']},
 {code:'Q-05',title:'انضباط شغلی',sources:'حضور و غیاب، تأخیر، غیبت و نظم اداری',criteria:['حضور به‌موقع','رعایت برنامه کاری','اعلام به‌موقع غیبت','رعایت مقررات سازمان']},
 {code:'Q-06',title:'رعایت استانداردهای سلامت اول',sources:'چک‌لیست استانداردهای خدمت',criteria:['رعایت دستورالعمل‌های سازمان','تکمیل صحیح مستندات','رعایت الزامات ایمنی','رعایت فرآیندهای خدمت']},
 {code:'Q-07',title:'همکاری سازمانی',sources:'گزارش همکاری واحدها',criteria:['پاسخ‌گویی مناسب','همکاری با واحد عملیات','همکاری با سوپروایزر','مشارکت در حل مسائل پرونده']},
 {code:'Q-08',title:'توسعه حرفه‌ای و مشارکت آموزشی',sources:'سوابق آموزش، آزمون‌ها و بازآموزی',criteria:['حضور در آموزش‌ها','قبولی در آزمون‌ها','مشارکت در انتقال تجربه','به‌روزرسانی دانش حرفه‌ای']}
];

const P=[
 ['P-01','رضایت کامل خانواده',5],['P-02','تمدید اول قرارداد',10],['P-03','تمدید دوم قرارداد',15],['P-04','تمدید سوم و بیشتر',20],
 ['P-05','عدم وجود شکایت در دوره ارزیابی',5],['P-06','عدم تعویض مراقب در طول قرارداد',5],['P-07','پایان موفق قرارداد بدون حاشیه',5],
 ['P-08','حضور در هر کارگاه بازآموزی',10],['P-09','قبولی در آزمون هر کارگاه',5],['P-10','کسب نمره ممتاز در آزمون',10],
 ['P-11','دوره آموزشی خارج سازمان مورد تأیید',10],['P-12','تدریس در کارگاه‌های سلامت اول',15],['P-13','تقدیر رسمی خانواده',10],
 ['P-14','تقدیر مدیریت',15],['P-15','همکاری مؤثر در شرایط بحرانی',10],['P-16','رعایت کامل استانداردها در ارزیابی نظارت',5],
 ['P-17','معرفی مراقب واجد شرایط',5],['P-18','معرفی مشتری جدید',10],['P-19','مشارکت در پروژه‌های سازمانی',10],
 ['P-20','پیشنهاد مؤثر برای بهبود خدمات',5],['P-21','مراقب برتر ماه',20],['P-22','مراقب برتر فصل',30],['P-23','مراقب برتر سال',50]
].map(([code,title,points])=>({code,title,points,type:'P'}));

const N=[
 ['N-01','تأخیر غیرموجه',-2],['N-02','غیبت غیرموجه',-10],['N-03','لغو شیفت بدون اطلاع قبلی',-10],['N-04','عدم رعایت برنامه کاری',-5],
 ['N-05','شکایت تأییدشده خانواده',-15],['N-06','قصور حرفه‌ای خفیف',-10],['N-07','قصور حرفه‌ای متوسط',-20],['N-08','قصور حرفه‌ای شدید',-30],
 ['N-09','ثبت ناقص یا نادرست گزارش‌ها',-5],['N-10','عدم حضور در کارگاه الزامی',-10],['N-11','عدم شرکت در آزمون',-5],
 ['N-12','عدم کسب حداقل نمره آزمون',-5],['N-13','بی‌احترامی به سالمند',-30],['N-14','بی‌احترامی به خانواده',-20],
 ['N-15','بی‌احترامی به همکاران یا کارکنان سلامت اول',-15],['N-16','عدم رعایت پوشش یا آراستگی حرفه‌ای',-5],
 ['N-17','نقض محرمانگی اطلاعات',-30],['N-18','عدم همکاری با واحدهای سازمان',-10],['N-19','عدم پاسخ‌گویی به تماس ضروری سازمان',-5],
 ['N-20','ارائه اطلاعات نادرست',-20]
].map(([code,title,points])=>({code,title,points,type:'N'}));

const CPD=[
 ['CPD-01','حضور در هر کارگاه بازآموزی',10],['CPD-02','قبولی در آزمون هر کارگاه',5],['CPD-03','کسب نمره ممتاز در آزمون',5],
 ['CPD-04','شرکت در دوره آموزشی مورد تأیید سلامت اول',10],['CPD-05','تدریس در کارگاه‌های سلامت اول',15],
 ['CPD-06','ارائه مقاله یا محتوای آموزشی مورد تأیید',10],['CPD-07','مشارکت در پروژه آموزشی سازمان',10],['CPD-08','مربیگری مراقبین جدید',10]
].map(([code,title,credit])=>({code,title,credit}));

const RANKS=[
 {code:'R-1',title:'ممتاز',stars:5},{code:'R-2',title:'ارشد',stars:4},{code:'R-3',title:'حرفه‌ای',stars:3},
 {code:'R-4',title:'پایه',stars:2},{code:'R-5',title:'مشروط',stars:1}
];

const LICENSE_STATUSES=['فعال','در حال تمدید','تعلیق‌شده','منقضی','ابطال‌شده'];
const CASE_STATUSES=[['C-01','شکایت ناقص'],['C-02','خارج از دامنه خدمات'],['C-03','قابل رسیدگی']];
const CASE_RESULTS=[['C-11','شکایت مردود'],['C-12','شکایت وارد بدون تخلف حرفه‌ای'],['C-13','تخلف احراز شد'],['C-14','ارجاع به کمیته ارزیابی'],['C-15','ارجاع به کمیته انضباطی']];
const SEVERITIES=[['S-1','خفیف'],['S-2','متوسط'],['S-3','شدید'],['S-4','بحرانی']];
const ACTIONS=[['A-01','ثبت بدون اقدام'],['A-02','تذکر شفاهی'],['A-03','اخطار کتبی'],['A-04','کسر امتیاز'],['A-05','الزام به بازآموزی'],['A-06','تعلیق موقت'],['A-07','ارجاع به کمیته ارزیابی'],['A-08','ابطال پروانه صلاحیت'],['A-09','خاتمه همکاری']];

function blankCriterion(q){return {code:q.code,title:q.title,status:'نیازمند بررسی تکمیلی',score:null,notes:'',evidence:[],updatedAt:null}}
function newPeriod(caregiverId){return {id:uid('EV'),caregiverId,policyVersion:POLICY_VERSION,title:'دوره ارزیابی جدید',start:'',end:'',status:'پیش‌نویس',assessor:'کارشناس ارزیابی منابع انسانی',reviewer:'مسئول ارزیابی منابع انسانی',criteria:Object.fromEntries(Q.map(x=>[x.code,blankCriterion(x)])),createdAt:nowFa(),submittedAt:null,finalizedAt:null,finalScore:null}}
function defaultState(){const caregiver={id:'CP-1405-0001',name:'مریم حسینی',nationalId:'',serviceGroup:'مراقبت سالمند',fileStatus:'CP-01 فعال',phone:'09128668837',rank:{code:'',title:'در انتظار ارزیابی',stars:0,pri:null,decisionRef:'',validFrom:'',validTo:''},license:{number:'',status:'در حال تمدید',issuedAt:'',expiresAt:'',qr:'',decisionRef:''}};return {policy:{version:POLICY_VERSION,weights:Object.fromEntries(Q.map(x=>[x.code,null])),minPerformance:null,periodType:'سه‌ماهه',rankThresholds:Object.fromEntries(RANKS.map(x=>[x.code,null])),approvedBy:'',approvedAt:''},caregivers:[caregiver],periods:[newPeriod(caregiver.id)],events:[],training:[],complaints:[],appeals:[],correctiveActions:[],committeeDecisions:[],audit:[],meta:{createdAt:nowFa(),environment:'preview'}}}
function load(){try{const raw=localStorage.getItem(STORAGE_KEY);if(raw){const parsed=JSON.parse(raw);if(parsed?.policy?.version===POLICY_VERSION)return parsed}}catch{}const s=defaultState();localStorage.setItem(STORAGE_KEY,JSON.stringify(s));return s}
let state=load();
let ui={caregiverId:state.caregivers[0]?.id||'',periodId:state.periods[0]?.id||'',complaintId:''};
try{ui={...ui,...JSON.parse(localStorage.getItem(UI_KEY)||'{}')}}catch{}
function persist(action,detail=''){state.audit.unshift({id:uid('AUD'),at:nowFa(),actor:currentActor(),action,detail});state.audit=state.audit.slice(0,250);localStorage.setItem(STORAGE_KEY,JSON.stringify(state));localStorage.setItem(UI_KEY,JSON.stringify(ui))}
function currentActor(){try{return selectedRole==='caregiver'?'مراقب':selectedRole==='hr'?'کارشناس ارزیابی منابع انسانی':selectedRole==='recruiter'?'کارشناس جذب':'مدیر سامانه'}catch{return 'سامانه'}}
function caregiver(){return state.caregivers.find(x=>x.id===ui.caregiverId)||state.caregivers[0]}
function period(){return state.periods.find(x=>x.id===ui.periodId)||state.periods.find(x=>x.caregiverId===caregiver()?.id)}
function approvedEvents(type){return state.events.filter(x=>x.caregiverId===caregiver()?.id&&x.type===type&&x.status==='تأییدشده'&&(!period()||x.periodId===period().id))}
function netPN(){return approvedEvents('P').reduce((a,b)=>a+Number(b.points||0),0)+approvedEvents('N').reduce((a,b)=>a+Number(b.points||0),0)}
function cpdTotal(){return state.training.filter(x=>x.caregiverId===caregiver()?.id&&x.status==='تأییدشده').reduce((a,b)=>a+Number(b.credit||0),0)}
function workshops(){return state.training.filter(x=>x.caregiverId===caregiver()?.id&&x.status==='تأییدشده'&&x.code==='CPD-01').length}
function testsPassed(){return state.training.filter(x=>x.caregiverId===caregiver()?.id&&x.status==='تأییدشده'&&['CPD-02','CPD-03'].includes(x.code)).length}
function weightsReady(){const vals=Q.map(x=>Number(state.policy.weights[x.code]));return vals.every(x=>Number.isFinite(x)&&x>=0)&&Math.round(vals.reduce((a,b)=>a+b,0)*100)/100===100}
function evidenceReady(p=period()){return !!p&&Q.every(x=>{const c=p.criteria[x.code];return c&&c.score!==null&&Number(c.score)>=0&&Number(c.score)<=100&&c.evidence?.length>0&&c.status!=='نیازمند بررسی تکمیلی'})}
function qScore(p=period()){if(!p||!weightsReady()||!evidenceReady(p))return null;return Math.round(Q.reduce((sum,x)=>sum+Number(p.criteria[x.code].score)*Number(state.policy.weights[x.code])/100,0)*10)/10}
function licenseChecklist(){const c=caregiver(),p=period(),score=qScore(p),serious=state.complaints.some(x=>x.caregiverId===c.id&&['S-3','S-4'].includes(x.severity)&&!['C-11'].includes(x.result));return [['حداقل امتیاز عملکرد',state.policy.minPerformance!==null&&score!==null&&score>=Number(state.policy.minPerformance),state.policy.minPerformance===null?'حداقل مصوب ثبت نشده':`${fa(score||0)} از حداقل ${fa(state.policy.minPerformance)}`],['حداقل ۷۵ اعتبار بازآموزی',cpdTotal()>=75,`${fa(cpdTotal())} از ۷۵`],['حضور در حداقل ۵ کارگاه از ۶ کارگاه',workshops()>=5,`${fa(workshops())} از ۶`],['قبولی در آزمون‌های الزامی',testsPassed()>0,`${fa(testsPassed())} قبولی ثبت‌شده`],['نداشتن تخلف مؤثر',!serious,serious?'تخلف مؤثر باز وجود دارد':'فاقد تخلف مؤثر باز'],['بسته‌شدن اقدامات اصلاحی',!state.correctiveActions.some(x=>x.caregiverId===c.id&&x.status!=='بسته'),`${state.correctiveActions.filter(x=>x.caregiverId===c.id&&x.status!=='بسته').length} اقدام باز`]]}
function scoreStatus(){const p=period();if(!p)return ['بدون دوره','neutral'];if(p.status==='نهایی')return [`${fa(p.finalScore)} از ۱۰۰`,'good'];if(p.status==='در انتظار کنترل')return ['در انتظار کنترل','warn'];return ['در حال ارزیابی','neutral']}
function stars(n){return `<span class="ev-stars">${[1,2,3,4,5].map(i=>`<i class="${i<=n?'on':''}">★</i>`).join('')}</span>`}
function badge(text,tone='neutral'){return `<span class="ev-badge ${tone}">${esc(text)}</span>`}
function empty(title,text){return `<div class="ev-empty"><span data-icon="file"></span><strong>${esc(title)}</strong><small>${esc(text)}</small></div>`}
function setPage(title,subtitle,html){$('#pageTitle').textContent=title;$('#pageSubtitle').textContent=subtitle;$('#content').innerHTML=`<section class="module-page ev-module">${html}</section>`;hydrateIcons($('#content'));bindCommon()}
function kpi(label,value,sub,tone='green'){return `<article class="ev-kpi ${tone}"><small>${label}</small><strong>${value}</strong><span>${sub}</span></article>`}
function periodOptions(){return state.periods.filter(x=>x.caregiverId===caregiver().id).map(x=>`<option value="${x.id}" ${x.id===ui.periodId?'selected':''}>${esc(x.title)} • ${esc(x.status)}</option>`).join('')}
function caregiverOptions(){return state.caregivers.map(x=>`<option value="${x.id}" ${x.id===ui.caregiverId?'selected':''}>${esc(x.name)} • ${x.id}</option>`).join('')}

function professionalScorecard(readonly=true){const c=caregiver(),p=period(),official=p?.status==='نهایی',pn=netPN(),cpd=cpdTotal(),rank=c.rank||{},license=c.license||{},sc=scoreStatus();const qCards=Q.map(x=>{const r=p?.criteria?.[x.code]||blankCriterion(x);return `<article class="ev-q-card" data-qview="${x.code}"><header><div><b>${x.code}</b><strong>${esc(x.title)}</strong></div><span>${r.score===null?'—':fa(r.score)}</span></header><div class="ev-progress"><i style="width:${r.score===null?0:Math.max(0,Math.min(100,r.score))}%"></i></div><footer>${badge(r.status,r.status==='تکمیل'?'good':r.status==='عدم احراز'?'bad':'warn')}<small>${fa(r.evidence?.length||0)} مستند</small></footer></article>`}).join('');const ledger=[...approvedEvents('P'),...approvedEvents('N')].sort((a,b)=>(b.createdAt||'').localeCompare(a.createdAt||''));const checks=licenseChecklist();return `<section class="ev-page-head"><div><span>کارنامه حرفه‌ای رسمی • ${POLICY_VERSION}</span><h2>${esc(c.name)}</h2><p>کد حرفه‌ای ${c.id} • ${esc(c.serviceGroup)} • ارزیاب: ${esc(p?.assessor||'تعیین نشده')}</p></div><div class="ev-head-actions"><select id="evPeriodSelect">${periodOptions()}</select>${readonly&&official?`<button class="btn outline" id="newAppeal">درخواست بازبینی</button>`:''}</div></section><section class="ev-kpis">${kpi('امتیاز عملکرد Q',official?`${fa(p.finalScore)} / ۱۰۰`:sc[0],official?'کارنامه نهایی':p?.status||'بدون دوره','green')}${kpi('خالص امتیازات P/N',`${pn>0?'+':''}${fa(pn)}`,`${fa(approvedEvents('P').length)} مثبت • ${fa(approvedEvents('N').length)} منفی`,pn<0?'red':'blue')}${kpi('اعتبار بازآموزی',`${fa(cpd)} / ۷۵`,`${fa(workshops())} کارگاه از ۶`,'orange')}${kpi('رتبه و ستاره',rank.code?`${rank.code} • ${esc(rank.title)}`:'در انتظار مصوبه',stars(rank.stars||0),'purple')}${kpi('پروانه صلاحیت',esc(license.status||'ثبت نشده'),license.expiresAt?`انقضا ${esc(license.expiresAt)}`:'تاریخ اعتبار ثبت نشده','green')}${kpi('اقدام جاری',state.correctiveActions.some(x=>x.caregiverId===c.id&&x.status!=='بسته')?'اقدام اصلاحی باز':'بدون اقدام باز',`${state.correctiveActions.filter(x=>x.caregiverId===c.id&&x.status!=='بسته').length} مورد`,'blue')}</section>${!official?`<div class="ev-callout warn"><strong>این کارنامه هنوز رسمی نیست.</strong><span>تا زمان تکمیل مستندات، کنترل مسئول ارزیابی و نهایی‌سازی، هیچ امتیاز یا رتبه‌ای رسمی محسوب نمی‌شود.</span></div>`:''}<section class="ev-grid two"><article class="surface"><div class="surface-head"><div><h3>هشت شاخص حرفه‌ای Q</h3><p>هر شاخص فقط با منبع مجاز و مستند قابل امتیازدهی است.</p></div></div><div class="ev-q-grid">${qCards}</div></article><article class="surface"><div class="surface-head"><div><h3>وضعیت تمدید پروانه</h3><p>کنترل خودکار الزامات مصوب سند</p></div></div><div class="ev-checklist">${checks.map(x=>`<div><span class="${x[1]?'ok':'no'}">${x[1]?'✓':'!'}</span><div><strong>${esc(x[0])}</strong><small>${esc(x[2])}</small></div></div>`).join('')}</div></article></section><section class="ev-grid two"><article class="surface"><div class="surface-head"><div><h3>دفتر گردش امتیازات حرفه‌ای</h3><p>رویدادهای تأییدشده P و N</p></div></div>${ledger.length?`<div class="ev-ledger">${ledger.map(x=>`<div><span class="${x.type==='P'?'plus':'minus'}">${x.points>0?'+':''}${fa(x.points)}</span><div><strong>${x.code} • ${esc(x.title)}</strong><small>${esc(x.contract||'بدون قرارداد')} • ${esc(x.date||'')} • ${esc(x.unit||'')}</small></div>${badge(x.status,'good')}</div>`).join('')}</div>`:empty('رویداد تأییدشده‌ای وجود ندارد','پس از تأیید کارشناس ارزیابی، امتیازات در این دفتر ثبت می‌شوند.')}</article><article class="surface"><div class="surface-head"><div><h3>سوابق دوره‌های ارزیابی</h3><p>نسخه‌های رسمی و قابل اعتراض</p></div></div><div class="ev-period-list">${state.periods.filter(x=>x.caregiverId===c.id).map(x=>`<button data-period="${x.id}" class="${x.id===ui.periodId?'active':''}"><div><strong>${esc(x.title)}</strong><small>${esc(x.start||'—')} تا ${esc(x.end||'—')}</small></div>${badge(x.status,x.status==='نهایی'?'good':x.status==='در انتظار کنترل'?'warn':'neutral')}</button>`).join('')}</div></article></section>`}

function caregiverDashboard(){const c=caregiver(),p=period(),rank=c.rank||{},license=c.license||{};setPage('داشبورد مراقب','کارنامه حرفه‌ای، قرارداد، پرداخت و آموزش',`<section class="role-hero caregiver-hero-panel ev-care-hero"><div><span class="cp-eyebrow">پرونده حرفه‌ای ${c.id}</span><h2>سلام ${esc(c.name.split(' ')[0])}، وضعیت حرفه‌ای شما شفاف و قابل پیگیری است</h2><p>ارزیابی فقط پس از بررسی مستندات توسط کارشناس ارزیابی منابع انسانی و تأیید مسئول ارزیابی رسمی می‌شود.</p></div><div>${stars(rank.stars||0)}<strong>${rank.code?`${rank.code} • ${esc(rank.title)}`:'در انتظار ارزیابی رسمی'}</strong></div></section><section class="ev-kpis">${kpi('وضعیت کارنامه',scoreStatus()[0],p?.status||'بدون دوره')}${kpi('اعتبار CPD',`${fa(cpdTotal())} / ۷۵`,`${fa(workshops())} کارگاه`,'orange')}${kpi('پروانه صلاحیت',esc(license.status||'ثبت نشده'),license.expiresAt||'تاریخ اعتبار ثبت نشده','green')}${kpi('اعتراض باز',fa(state.appeals.filter(x=>x.caregiverId===c.id&&!['مختومه','رد اعتراض','پذیرفته'].includes(x.status)).length),'قابل رهگیری','purple')}</section><section class="ev-grid two"><article class="surface"><div class="surface-head"><div><h3>آخرین وضعیت ارزیابی</h3><p>${esc(p?.title||'بدون دوره')}</p></div><button class="btn primary" data-open-care-score>مشاهده کارنامه</button></div><div class="ev-status-flow">${['پیش‌نویس','در انتظار کنترل','نهایی'].map((x,i)=>`<div class="${p?.status===x?'active':''}"><i>${fa(i+1)}</i><span>${x}</span></div>`).join('')}</div></article><article class="surface"><div class="surface-head"><div><h3>اصل شفافیت</h3><p>هیچ تصمیمی صرفاً بر اساس گزارش شفاهی ثبت نمی‌شود.</p></div></div><div class="ev-callout"><strong>حق بازبینی محفوظ است</strong><span>پس از ابلاغ کارنامه نهایی، تا ۷ روز کاری امکان ثبت درخواست بازبینی وجود دارد.</span></div></article></section>`)}
function readonlyFilePage(){setPage('پرونده حرفه‌ای مراقبین','دسترسی مشاهده بر اساس نقش سازمانی',`<section class="ev-toolbar"><label>مراقب<select id="evCaregiverSelect">${caregiverOptions()}</select></label></section>${professionalScorecard(true)}`)}
function hrDashboard(){const drafts=state.periods.filter(x=>x.status==='پیش‌نویس').length,reviews=state.periods.filter(x=>x.status==='در انتظار کنترل').length,missing=state.periods.filter(x=>!evidenceReady(x)).length,appeals=state.appeals.filter(x=>!['مختومه','رد اعتراض','پذیرفته'].includes(x.status)).length;setPage('داشبورد منابع انسانی','حقوق، قرارداد و نظام ارزیابی مراقبین',`<section class="role-hero ev-hr-hero"><div><span class="cp-eyebrow">مسئولیت ارزیابی کارشناسی</span><h2>میزکار نظام ارزیابی مراقبین</h2><p>ثبت شاخص‌ها، کنترل مستندات، امتیازات P/N، بازآموزی، رتبه، پروانه و اعتراضات در یک مسیر قابل رهگیری.</p></div><button class="btn primary" data-open-workbench>ورود به میزکار ارزیابی</button></section><section class="ev-kpis">${kpi('پیش‌نویس‌ها',fa(drafts),'در حال تکمیل')}${kpi('در انتظار کنترل',fa(reviews),'نیازمند تأیید مسئول','orange')}${kpi('مستندات ناقص',fa(missing),'قابل نهایی‌سازی نیست','red')}${kpi('اعتراضات باز',fa(appeals),'مهلت‌دار','purple')}</section><section class="ev-grid three">${['ارزیابی دوره‌ای','امتیازات P و N','شکایات و تخلفات','بازآموزی و CPD','رتبه و پروانه','کنترل کیفیت'].map((x,i)=>`<button class="surface ev-module-card" data-hr-go="${x}"><span data-icon="${['chart','badge','alert','book','shield','settings'][i]}"></span><strong>${x}</strong><small>فرآند مستند و قابل حسابرسی</small></button>`).join('')}</section>`)}
function workbench(){const c=caregiver(),p=period(),complete=Q.filter(x=>p?.criteria?.[x.code]?.score!==null&&p.criteria[x.code].evidence?.length).length;const qHtml=Q.map(x=>{const r=p.criteria[x.code];return `<article class="ev-editor-card" data-q="${x.code}"><header><div><b>${x.code}</b><strong>${esc(x.title)}</strong><small>منبع: ${esc(x.sources)}</small></div>${badge(r.status,r.status==='تکمیل'?'good':r.status==='عدم احراز'?'bad':'warn')}</header><div class="ev-editor-grid"><label>امتیاز ۰ تا ۱۰۰<input class="q-score" type="number" min="0" max="100" value="${r.score??''}"></label><label>وضعیت<select class="q-status">${['نیازمند بررسی تکمیلی','تکمیل','عدم احراز','غیرقابل ارزیابی'].map(v=>`<option ${r.status===v?'selected':''}>${v}</option>`).join('')}</select></label><label class="wide">یادداشت ارزیاب<textarea class="q-notes" rows="2">${esc(r.notes)}</textarea></label></div><div class="ev-evidence"><div>${r.evidence.map((e,i)=>`<span>${esc(e.source)} • ${esc(e.ref)} <button data-del-evidence="${x.code}:${i}">×</button></span>`).join('')||'<small>هنوز مستندی متصل نشده است.</small>'}</div><div><select class="e-source">${x.sources.split('،').map(s=>`<option>${esc(s.trim())}</option>`).join('')}</select><input class="e-ref" placeholder="شناسه یا شرح مستند"><button class="btn outline" data-add-evidence="${x.code}">افزودن مستند</button></div></div><button class="btn primary" data-save-q="${x.code}">ذخیره شاخص</button></article>`}).join('');const score=qScore(p);setPage('میزکار ارزیابی','کارشناس ارزیابی منابع انسانی',`<section class="ev-toolbar"><label>مراقب<select id="evCaregiverSelect">${caregiverOptions()}</select></label><label>دوره<select id="evPeriodSelect">${periodOptions()}</select></label><button class="btn outline" id="createPeriod">ایجاد دوره جدید</button></section><section class="ev-page-head"><div><span>${POLICY_VERSION}</span><h2>${esc(c.name)} • ${esc(p.title)}</h2><p>${esc(p.start||'تاریخ شروع تعیین نشده')} تا ${esc(p.end||'تاریخ پایان تعیین نشده')} • ارزیاب: ${esc(p.assessor)}</p></div>${badge(p.status,p.status==='نهایی'?'good':p.status==='در انتظار کنترل'?'warn':'neutral')}</section><section class="ev-kpis">${kpi('شاخص تکمیل‌شده',`${fa(complete)} / ۸`,'هر شاخص با مستند')}${kpi('وزن‌ها',weightsReady()?'۱۰۰٪ معتبر':'ناقص','در کنترل کیفیت تعیین می‌شود',weightsReady()?'green':'red')}${kpi('امتیاز محاسبه‌شده',score===null?'—':`${fa(score)} / ۱۰۰`,'فقط پس از تکمیل مستندات','blue')}${kpi('رویدادهای تأییدشده',fa(approvedEvents('P').length+approvedEvents('N').length),'P و N','purple')}</section><div class="ev-callout ${weightsReady()?'':'warn'}"><strong>${weightsReady()?'ضرایب مصوب آماده محاسبه‌اند':'ماتریس وزن شاخص‌ها هنوز ثبت نشده است'}</strong><span>${weightsReady()?'برای نهایی‌سازی باید همه شاخص‌ها مستند و کنترل شوند.':'برای جلوگیری از عددسازی، نهایی‌سازی تا ثبت ضرایب مصوب در کنترل کیفیت مسدود است.'}</span></div><section class="ev-editor-list">${qHtml}</section><section class="surface ev-submit-box"><div><h3>کنترل و نهایی‌سازی</h3><p>ابتدا پرونده برای کنترل مسئول ارزیابی ارسال می‌شود؛ سپس فقط پرونده کامل قابل نهایی‌شدن است.</p></div><div><button class="btn outline" id="submitReview">ارسال برای کنترل</button><button class="btn primary" id="finalizeEvaluation">تأیید و نهایی‌سازی</button></div></section>`)}
function periodsPage(){setPage('دوره‌های ارزیابی','مدیریت دوره‌های مستمر و تاریخچه کارنامه‌ها',`<section class="ev-toolbar"><label>مراقب<select id="evCaregiverSelect">${caregiverOptions()}</select></label><button class="btn primary" id="createPeriod">ایجاد دوره ارزیابی</button></section><article class="surface table-wrap"><table class="data-table"><thead><tr><th>شناسه</th><th>عنوان</th><th>بازه</th><th>ارزیاب</th><th>وضعیت</th><th>امتیاز نهایی</th><th></th></tr></thead><tbody>${state.periods.filter(x=>x.caregiverId===caregiver().id).map(x=>`<tr><td>${x.id}</td><td>${esc(x.title)}</td><td>${esc(x.start||'—')} تا ${esc(x.end||'—')}</td><td>${esc(x.assessor)}</td><td>${badge(x.status,x.status==='نهایی'?'good':x.status==='در انتظار کنترل'?'warn':'neutral')}</td><td>${x.finalScore===null?'—':fa(x.finalScore)}</td><td><button class="cp-link-btn" data-open-period="${x.id}">بازکردن</button></td></tr>`).join('')}</tbody></table></article>`)}
function eventsPage(){const all=state.events.filter(x=>x.caregiverId===caregiver().id&&x.periodId===period().id);setPage('امتیازات P و N','ثبت، بررسی و تأیید رویدادهای حرفه‌ای',`<section class="ev-toolbar"><label>مراقب<select id="evCaregiverSelect">${caregiverOptions()}</select></label><label>دوره<select id="evPeriodSelect">${periodOptions()}</select></label></section><section class="ev-grid two"><article class="surface"><div class="surface-head"><div><h3>ثبت رویداد امتیازی</h3><p>هر رویداد فقط یک‌بار و با مستند معتبر ثبت می‌شود.</p></div></div><form class="ev-form" id="eventForm"><label>کد و عنوان<select name="catalog">${[...P,...N].map(x=>`<option value="${x.type}|${x.code}">${x.code} • ${esc(x.title)} • ${x.points>0?'+':''}${fa(x.points)}</option>`).join('')}</select></label><label>قرارداد مرتبط<input name="contract" required placeholder="CT-1405-..."></label><label>تاریخ وقوع<input name="date" required placeholder="۱۴۰۵/۰۵/۰۱"></label><label>واحد ثبت‌کننده<select name="unit"><option>منابع انسانی</option><option>عملیات</option><option>نظارت</option><option>CRM</option><option>آموزش</option><option>سوپروایزر</option></select></label><label class="wide">مستند قابل استناد<textarea name="evidence" required rows="3" placeholder="شناسه گزارش، تیکت، فرم یا سند"></textarea></label><button class="btn primary wide">ثبت پیشنهادی</button></form></article><article class="surface"><div class="surface-head"><div><h3>جمع دوره</h3><p>فقط رویدادهای تأییدشده در محاسبه وارد می‌شوند.</p></div></div><div class="ev-summary-numbers"><div><strong>+${fa(approvedEvents('P').reduce((a,b)=>a+b.points,0))}</strong><span>مثبت</span></div><div><strong>${fa(approvedEvents('N').reduce((a,b)=>a+b.points,0))}</strong><span>منفی</span></div><div><strong>${netPN()>0?'+':''}${fa(netPN())}</strong><span>خالص</span></div></div></article></section><article class="surface table-wrap"><div class="surface-head"><div><h3>دفتر رویدادهای دوره</h3><p>ثبت تکراری با تطبیق کد، قرارداد، تاریخ و مستند مسدود می‌شود.</p></div></div>${all.length?`<table class="data-table"><thead><tr><th>کد</th><th>عنوان</th><th>قرارداد</th><th>امتیاز</th><th>واحد</th><th>وضعیت</th><th>عملیات</th></tr></thead><tbody>${all.map(x=>`<tr><td>${x.code}</td><td>${esc(x.title)}<small class="ev-cell-note">${esc(x.evidence)}</small></td><td>${esc(x.contract)}</td><td class="${x.points>0?'ev-plus':'ev-minus'}">${x.points>0?'+':''}${fa(x.points)}</td><td>${esc(x.unit)}</td><td>${badge(x.status,x.status==='تأییدشده'?'good':x.status==='ردشده'?'bad':'warn')}</td><td>${x.status==='پیشنهادی'?`<button class="cp-link-btn" data-approve-event="${x.id}">تأیید</button><button class="cp-link-btn danger" data-reject-event="${x.id}">رد</button>`:'—'}</td></tr>`).join('')}</tbody></table>`:empty('رویدادی ثبت نشده','فرم بالا برای ثبت رویداد مثبت یا منفی آماده است.')}</article>`)}
function complaintsPage(){const selected=state.complaints.find(x=>x.id===ui.complaintId)||state.complaints.find(x=>x.caregiverId===caregiver().id);if(selected)ui.complaintId=selected.id;setPage('شکایات و تخلفات','SOP2 • رسیدگی مستند، دفاع و اقدام اصلاحی',`<section class="ev-toolbar"><label>مراقب<select id="evCaregiverSelect">${caregiverOptions()}</select></label></section><section class="ev-grid two"><article class="surface"><div class="surface-head"><div><h3>ثبت شکایت رسمی</h3><p>سامانه پس از ثبت، C-ID ایجاد می‌کند.</p></div></div><form class="ev-form" id="complaintForm"><label>شماره پرونده خدمت<input name="contract" required></label><label>مشخصات شاکی<input name="complainant" required></label><label>تاریخ و ساعت وقوع<input name="occurredAt" required></label><label>محل خدمت<input name="location" required></label><label>سطح فوریت<select name="urgency"><option>عادی</option><option>فوری</option><option>بحرانی</option></select></label><label class="wide">شرح کامل موضوع<textarea name="description" required rows="4"></textarea></label><label class="wide">مستندات اولیه<textarea name="evidence" required rows="2"></textarea></label><button class="btn primary wide">ثبت شکایت</button></form></article><article class="surface"><div class="surface-head"><div><h3>پرونده‌های شکایت</h3><p>غربالگری باید حداکثر ظرف دو روز کاری انجام شود.</p></div></div><div class="ev-case-list">${state.complaints.filter(x=>x.caregiverId===caregiver().id).map(x=>`<button data-case="${x.id}" class="${selected?.id===x.id?'active':''}"><div><strong>${x.id}</strong><small>${esc(x.contract)} • ${esc(x.urgency)}</small></div>${badge(x.status,x.status==='مختومه'?'good':'warn')}</button>`).join('')||empty('شکایتی وجود ندارد','پرونده رسمی ثبت نشده است.')}</div></article></section>${selected?`<article class="surface ev-case-detail"><div class="surface-head"><div><h3>${selected.id} • ${esc(selected.contract)}</h3><p>${esc(selected.description)}</p></div>${badge(selected.status,'warn')}</div><div class="ev-case-timeline">${['ثبت شکایت','غربالگری','جمع‌آوری مستندات','اخذ دفاعیات','ارزیابی تخلف','اقدام اصلاحی','ابلاغ نتیجه'].map((x,i)=>`<div class="${Number(selected.step||1)>i?'done':''}"><i>${Number(selected.step||1)>i?'✓':fa(i+1)}</i><span>${x}</span></div>`).join('')}</div><form class="ev-form" id="caseUpdateForm"><label>وضعیت غربالگری<select name="screening">${CASE_STATUSES.map(x=>`<option value="${x[0]}" ${selected.screening===x[0]?'selected':''}>${x[0]} • ${x[1]}</option>`).join('')}</select></label><label>شدت<select name="severity"><option value="">تعیین نشده</option>${SEVERITIES.map(x=>`<option value="${x[0]}" ${selected.severity===x[0]?'selected':''}>${x[0]} • ${x[1]}</option>`).join('')}</select></label><label>نتیجه<select name="result"><option value="">تعیین نشده</option>${CASE_RESULTS.map(x=>`<option value="${x[0]}" ${selected.result===x[0]?'selected':''}>${x[0]} • ${x[1]}</option>`).join('')}</select></label><label>اقدام<select name="action"><option value="">تعیین نشده</option>${ACTIONS.map(x=>`<option value="${x[0]}" ${selected.action===x[0]?'selected':''}>${x[0]} • ${x[1]}</option>`).join('')}</select></label><label class="wide">دفاعیات مراقب<textarea name="defense" rows="3">${esc(selected.defense||'')}</textarea></label><label class="wide">گزارش بررسی و مستندات تکمیلی<textarea name="review" rows="3">${esc(selected.review||'')}</textarea></label><button class="btn primary wide">ذخیره و پیشبرد پرونده</button></form></article>`:''}`)}
function trainingPage(){const records=state.training.filter(x=>x.caregiverId===caregiver().id);setPage('بازآموزی و CPD','ثبت اعتبار آموزشی و کنترل الزامات سالانه',`<section class="ev-toolbar"><label>مراقب<select id="evCaregiverSelect">${caregiverOptions()}</select></label></section><section class="ev-kpis">${kpi('اعتبار کسب‌شده',`${fa(cpdTotal())} / ۷۵`,'حداقل سالانه')}${kpi('کارگاه‌ها',`${fa(workshops())} / ۶`,'حداقل حضور ۵','orange')}${kpi('قبولی آزمون',fa(testsPassed()),'آزمون‌های ثبت‌شده','blue')}${kpi('وضعیت تمدید',cpdTotal()>=75&&workshops()>=5&&testsPassed()>0?'احراز آموزشی':'ناقص','الزامات پروانه','purple')}</section><section class="ev-grid two"><article class="surface"><div class="surface-head"><div><h3>ثبت فعالیت آموزشی</h3><p>اعتبار فقط پس از تأیید واحد آموزش قطعی می‌شود.</p></div></div><form class="ev-form" id="trainingForm"><label>فعالیت<select name="code">${CPD.map(x=>`<option value="${x.code}">${x.code} • ${esc(x.title)} • ${fa(x.credit)} اعتبار</option>`).join('')}</select></label><label>عنوان دوره یا فعالیت<input name="title" required></label><label>تاریخ<input name="date" required placeholder="۱۴۰۵/۰۵/۰۱"></label><label>نمره آزمون<input name="score" type="number" min="0" max="100"></label><label>درصد حضور<input name="attendance" type="number" min="0" max="100" value="100"></label><label>شناسه مستند<input name="evidence" required></label><button class="btn primary wide">ثبت برای تأیید آموزش</button></form></article><article class="surface"><div class="surface-head"><div><h3>مسیر الزامات سالانه</h3><p>اعتبارها فقط در دوره جاری معتبرند.</p></div></div><div class="ev-gauge"><div style="--p:${Math.min(100,cpdTotal()/75*100)}"><strong>${fa(Math.round(Math.min(100,cpdTotal()/75*100)))}٪</strong></div><p>${cpdTotal()>=75?'حداقل اعتبار احراز شده است.':`${fa(75-cpdTotal())} اعتبار تا حداقل سالانه باقی مانده است.`}</p></div></article></section><article class="surface table-wrap"><table class="data-table"><thead><tr><th>کد</th><th>عنوان</th><th>تاریخ</th><th>حضور</th><th>نمره</th><th>اعتبار</th><th>وضعیت</th><th></th></tr></thead><tbody>${records.map(x=>`<tr><td>${x.code}</td><td>${esc(x.title)}<small class="ev-cell-note">${esc(x.evidence)}</small></td><td>${esc(x.date)}</td><td>${fa(x.attendance)}٪</td><td>${x.score===''?'—':fa(x.score)}</td><td>${fa(x.credit)}</td><td>${badge(x.status,x.status==='تأییدشده'?'good':'warn')}</td><td>${x.status==='پیشنهادی'?`<button class="cp-link-btn" data-approve-training="${x.id}">تأیید آموزش</button>`:'—'}</td></tr>`).join('')}</tbody></table></article>`)}
function rankLicensePage(){const c=caregiver(),checks=licenseChecklist(),rank=c.rank||{},license=c.license||{};setPage('رتبه و پروانه','ثبت تصمیم رتبه‌بندی و کنترل شرایط صلاحیت',`<section class="ev-toolbar"><label>مراقب<select id="evCaregiverSelect">${caregiverOptions()}</select></label><label>دوره<select id="evPeriodSelect">${periodOptions()}</select></label></section><section class="ev-grid two"><article class="surface"><div class="surface-head"><div><h3>رتبه حرفه‌ای</h3><p>PRI و رتبه فقط با شماره تصمیم کمیته یا مسئول مجاز ثبت می‌شود.</p></div></div><div class="ev-rank-show">${stars(rank.stars||0)}<h2>${rank.code?`${rank.code} • ${esc(rank.title)}`:'در انتظار تصمیم'}</h2><p>PRI: ${rank.pri===null||rank.pri===undefined?'—':fa(rank.pri)}</p></div><form class="ev-form" id="rankForm"><label>شاخص PRI<input name="pri" type="number" min="0" max="100" value="${rank.pri??''}" required></label><label>رتبه<select name="code"><option value="">انتخاب کنید</option>${RANKS.map(x=>`<option value="${x.code}" ${rank.code===x.code?'selected':''}>${x.code} • ${x.title} • ${fa(x.stars)} ستاره</option>`).join('')}</select></label><label>شروع اعتبار<input name="validFrom" value="${esc(rank.validFrom||'')}"></label><label>پایان اعتبار<input name="validTo" value="${esc(rank.validTo||'')}"></label><label class="wide">شماره تصمیم یا مصوبه<input name="decisionRef" value="${esc(rank.decisionRef||'')}" required></label><button class="btn primary wide">ثبت رتبه با سابقه</button></form></article><article class="surface"><div class="surface-head"><div><h3>پروانه صلاحیت حرفه‌ای</h3><p>صدور، تمدید، تعلیق یا ابطال با ثبت مرجع تصمیم</p></div></div><div class="ev-checklist">${checks.map(x=>`<div><span class="${x[1]?'ok':'no'}">${x[1]?'✓':'!'}</span><div><strong>${esc(x[0])}</strong><small>${esc(x[2])}</small></div></div>`).join('')}</div><form class="ev-form" id="licenseForm"><label>شماره پروانه<input name="number" value="${esc(license.number||'')}"></label><label>وضعیت<select name="status">${LICENSE_STATUSES.map(x=>`<option ${license.status===x?'selected':''}>${x}</option>`).join('')}</select></label><label>تاریخ صدور<input name="issuedAt" value="${esc(license.issuedAt||'')}"></label><label>تاریخ انقضا<input name="expiresAt" value="${esc(license.expiresAt||'')}"></label><label class="wide">مرجع تصمیم<input name="decisionRef" value="${esc(license.decisionRef||'')}" required></label><button class="btn primary wide">ثبت وضعیت پروانه</button></form></article></section>`)}
function appealsPage(isCaregiver=false){const rows=state.appeals.filter(x=>x.caregiverId===caregiver().id);setPage('اعتراضات و بازبینی','SOP6 • ثبت و رسیدگی مستقل',`${isCaregiver?`<section class="ev-callout"><strong>مهلت اعتراض</strong><span>پس از ابلاغ نتیجه نهایی، تا ۷ روز کاری می‌توانید درخواست بازبینی ثبت کنید.</span></section>`:`<section class="ev-toolbar"><label>مراقب<select id="evCaregiverSelect">${caregiverOptions()}</select></label></section>`}<section class="ev-grid two"><article class="surface"><div class="surface-head"><div><h3>${isCaregiver?'ثبت درخواست بازبینی':'اعتراض جدید یا تکمیل پرونده'}</h3><p>رسیدگی‌کننده نباید در تصمیم اولیه نقش مستقیم داشته باشد.</p></div></div><form class="ev-form" id="appealForm"><label>موضوع<select name="subject"><option>امتیاز شاخص‌ها</option><option>ثبت‌نشدن امتیاز مثبت</option><option>ثبت تخلف</option><option>میزان کسر امتیاز</option><option>رتبه حرفه‌ای</option><option>وضعیت پروانه</option><option>الزام به بازآموزی</option></select></label><label>دوره<select name="periodId">${periodOptions()}</select></label><label class="wide">شرح اعتراض<textarea name="description" required rows="4"></textarea></label><label class="wide">مستندات جدید<textarea name="evidence" rows="2"></textarea></label><button class="btn primary wide">ثبت اعتراض و دریافت AP-ID</button></form></article><article class="surface"><div class="surface-head"><div><h3>سوابق اعتراض</h3><p>تمام تغییرات وابسته پس از پذیرش دوباره محاسبه می‌شوند.</p></div></div><div class="ev-case-list">${rows.map(x=>`<button data-appeal="${x.id}"><div><strong>${x.id} • ${esc(x.subject)}</strong><small>${esc(x.createdAt)} • ${esc(x.description)}</small></div>${badge(x.status,x.status==='پذیرفته'?'good':x.status==='رد اعتراض'?'bad':'warn')}</button>`).join('')||empty('اعتراضی ثبت نشده','درخواست‌های بازبینی در این بخش قابل رهگیری هستند.')}</div></article></section>${!isCaregiver&&rows.length?`<article class="surface"><div class="surface-head"><div><h3>رسیدگی کارشناسی</h3><p>تصمیم باید مستند و توسط فرد مستقل ثبت شود.</p></div></div><form class="ev-form" id="appealDecisionForm"><label>پرونده<select name="id">${rows.map(x=>`<option value="${x.id}">${x.id} • ${esc(x.subject)}</option>`).join('')}</select></label><label>نتیجه<select name="status"><option>در حال بررسی</option><option>ارجاع به کمیته</option><option>رد اعتراض</option><option>پذیرفته</option><option>مختومه</option></select></label><label class="wide">شرح تصمیم<textarea name="decision" required rows="3"></textarea></label><label class="wide">مرجع و مستند تصمیم<input name="decisionRef" required></label><button class="btn primary wide">ثبت نتیجه بازبینی</button></form></article>`:''}`)}
function qualityPage(){const sum=Q.reduce((a,x)=>a+Number(state.policy.weights[x.code]||0),0);setPage('کنترل کیفیت ارزیابی','ضرایب، حداقل‌ها، نسخه مقررات و Audit Log',`<section class="ev-grid two"><article class="surface"><div class="surface-head"><div><h3>ماتریس وزن شاخص‌ها</h3><p>سند به ماتریس وزن اشاره دارد اما اعداد باید با مصوبه وارد شوند.</p></div>${badge(`${fa(sum)}٪ از ۱۰۰٪`,sum===100?'good':'bad')}</div><form class="ev-form" id="policyForm">${Q.map(x=>`<label>${x.code} • ${esc(x.title)}<input name="${x.code}" type="number" min="0" max="100" step="0.1" value="${state.policy.weights[x.code]??''}"></label>`).join('')}<label>حداقل امتیاز عملکرد برای تمدید<input name="minPerformance" type="number" min="0" max="100" value="${state.policy.minPerformance??''}"></label><label>نوع دوره ارزیابی<select name="periodType">${['ماهانه','سه‌ماهه','شش‌ماهه','سالانه'].map(x=>`<option ${state.policy.periodType===x?'selected':''}>${x}</option>`).join('')}</select></label><label>مرجع تصویب<input name="approvedBy" value="${esc(state.policy.approvedBy||'')}" required></label><label>تاریخ تصویب<input name="approvedAt" value="${esc(state.policy.approvedAt||'')}" required></label><button class="btn primary wide">ثبت تنظیمات نسخه‌دار</button></form></article><article class="surface"><div class="surface-head"><div><h3>قواعد غیرقابل دورزدن</h3><p>${POLICY_VERSION}</p></div></div><div class="ev-rule-list">${['بدون مستند، امتیاز ثبت نمی‌شود.','هر رویداد فقط یک بار امتیاز دارد.','شاخص ناقص با صفر جایگزین نمی‌شود.','تخلف بدون دفاعیات و مرجع رسیدگی قطعی نمی‌شود.','ثبت نهایی قفل است و اصلاح فقط با مجوز انجام می‌شود.','تمام مشاهده و ویرایش‌ها در Audit Log ثبت می‌شوند.'].map(x=>`<div><span>✓</span>${x}</div>`).join('')}</div></article></section><article class="surface table-wrap"><div class="surface-head"><div><h3>گزارش حسابرسی</h3><p>آخرین ۲۵۰ رویداد سامانه</p></div><button class="btn outline" id="exportAudit">خروجی JSON</button></div><table class="data-table"><thead><tr><th>زمان</th><th>کاربر</th><th>عملیات</th><th>جزئیات</th></tr></thead><tbody>${state.audit.slice(0,80).map(x=>`<tr><td>${esc(x.at)}</td><td>${esc(x.actor)}</td><td>${esc(x.action)}</td><td>${esc(x.detail)}</td></tr>`).join('')}</tbody></table></article>`)}
function renderEvalModule(label){if(label==='کارنامه حرفه‌ای')return setPage('کارنامه حرفه‌ای','مرجع رسمی ارزیابی عملکرد مراقب',professionalScorecard(true));if(label==='رتبه و پروانه')return setPage('رتبه و پروانه','وضعیت حرفه‌ای و صلاحیت',professionalScorecard(true));if(label==='اعتراضات و بازبینی')return appealsPage(true);if(label==='میزکار ارزیابی')return workbench();if(label==='پرونده حرفه‌ای مراقبین')return readonlyFilePage();if(label==='دوره‌های ارزیابی')return periodsPage();if(label==='امتیازات P و N')return eventsPage();if(label==='شکایات و تخلفات')return complaintsPage();if(label==='بازآموزی و CPD')return trainingPage();if(label==='رتبه‌بندی و پروانه')return rankLicensePage();if(label==='اعتراضات')return appealsPage(false);if(label==='کنترل کیفیت ارزیابی')return qualityPage();return false}
function createPeriodFromForm(){const c=caregiver(),p=newPeriod(c.id),title=prompt('عنوان دوره ارزیابی','ارزیابی سه‌ماهه دوم ۱۴۰۵');if(!title)return;p.title=title;p.start=prompt('تاریخ شروع','۱۴۰۵/۰۴/۰۱')||'';p.end=prompt('تاریخ پایان','۱۴۰۵/۰۶/۳۱')||'';state.periods.push(p);ui.periodId=p.id;persist('ایجاد دوره ارزیابی',`${p.id} برای ${c.id}`);workbench();showToast('دوره ایجاد شد','پرونده ارزیابی جدید آماده تکمیل است.')}
function showToast(title,text){try{toast(title,text)}catch{alert(`${title}\n${text}`)}}
function bindCommon(){$('#evCaregiverSelect')?.addEventListener('change',e=>{ui.caregiverId=e.target.value;const fp=state.periods.find(x=>x.caregiverId===ui.caregiverId);if(fp)ui.periodId=fp.id;localStorage.setItem(UI_KEY,JSON.stringify(ui));renderEvalModule($('#pageTitle').textContent==='پرونده حرفه‌ای مراقبین'?'پرونده حرفه‌ای مراقبین':labelFromTitle())});$('#evPeriodSelect')?.addEventListener('change',e=>{ui.periodId=e.target.value;localStorage.setItem(UI_KEY,JSON.stringify(ui));const t=$('#pageTitle').textContent;if(t.includes('کارنامه'))setPage('کارنامه حرفه‌ای','مرجع رسمی ارزیابی عملکرد مراقب',professionalScorecard(true));else if(t.includes('میزکار'))workbench();else if(t.includes('امتیازات'))eventsPage();else if(t.includes('رتبه'))rankLicensePage()});$$('[data-period]').forEach(b=>b.onclick=()=>{ui.periodId=b.dataset.period;localStorage.setItem(UI_KEY,JSON.stringify(ui));setPage('کارنامه حرفه‌ای','مرجع رسمی ارزیابی عملکرد مراقب',professionalScorecard(true))});$$('[data-qview]').forEach(b=>b.onclick=()=>{const x=Q.find(q=>q.code===b.dataset.qview),r=period()?.criteria?.[x.code];$('#drawerTitle').textContent=`${x.code} • ${x.title}`;$('#drawerBody').innerHTML=`<div class="ev-drawer"><h4>معیارهای رفتاری</h4>${x.criteria.map(c=>`<div class="ev-drawer-row"><span>✓</span>${esc(c)}</div>`).join('')}<h4>منابع مجاز</h4><p>${esc(x.sources)}</p><h4>مستندات دوره</h4>${r?.evidence?.length?r.evidence.map(e=>`<div class="ev-drawer-row"><span>•</span>${esc(e.source)} — ${esc(e.ref)}</div>`).join(''):'<p>مستندی ثبت نشده است.</p>'}</div>`;$('#drawer').classList.add('open');$('#drawerBackdrop').classList.remove('hidden')});$('[data-open-care-score]')?.addEventListener('click',()=>setPage('کارنامه حرفه‌ای','مرجع رسمی ارزیابی عملکرد مراقب',professionalScorecard(true)));$('[data-open-workbench]')?.addEventListener('click',workbench);$$('[data-hr-go]').forEach(b=>b.onclick=()=>{const map={'ارزیابی دوره‌ای':'میزکار ارزیابی','کنترل کیفیت':'کنترل کیفیت ارزیابی'};renderEvalModule(map[b.dataset.hrGo]||b.dataset.hrGo)});$('#createPeriod')?.addEventListener('click',createPeriodFromForm);$('#newAppeal')?.addEventListener('click',()=>appealsPage(true));bindWorkbench();bindEvents();bindComplaints();bindTraining();bindRank();bindAppeals();bindQuality()}
function labelFromTitle(){const t=$('#pageTitle')?.textContent||'';return t.includes('میزکار')?'میزکار ارزیابی':t.includes('دوره')?'دوره‌های ارزیابی':t.includes('امتیازات')?'امتیازات P و N':t.includes('شکایت')?'شکایات و تخلفات':t.includes('بازآموزی')?'بازآموزی و CPD':t.includes('رتبه')?'رتبه‌بندی و پروانه':t.includes('اعتراض')?'اعتراضات':t.includes('کنترل')?'کنترل کیفیت ارزیابی':'پرونده حرفه‌ای مراقبین'}
function bindWorkbench(){const p=period();if(!p)return;$$('[data-save-q]').forEach(b=>b.onclick=()=>{const card=b.closest('[data-q]'),code=b.dataset.saveQ,r=p.criteria[code];r.score=card.querySelector('.q-score').value===''?null:Number(card.querySelector('.q-score').value);r.status=card.querySelector('.q-status').value;r.notes=card.querySelector('.q-notes').value;r.updatedAt=nowFa();persist('ذخیره شاخص ارزیابی',`${p.id} • ${code}`);workbench();showToast('شاخص ذخیره شد',`${code} با سابقه تغییر ثبت شد.`)});$$('[data-add-evidence]').forEach(b=>b.onclick=()=>{const card=b.closest('[data-q]'),code=b.dataset.addEvidence,ref=card.querySelector('.e-ref').value.trim(),source=card.querySelector('.e-source').value;if(!ref)return showToast('مستند ناقص است','شناسه یا شرح مستند را وارد کنید.');p.criteria[code].evidence.push({id:uid('E'),source,ref,at:nowFa(),actor:currentActor()});persist('افزودن مستند ارزیابی',`${p.id} • ${code} • ${ref}`);workbench()});$$('[data-del-evidence]').forEach(b=>b.onclick=()=>{const [code,idx]=b.dataset.delEvidence.split(':');p.criteria[code].evidence.splice(Number(idx),1);persist('حذف مستند پیش‌نویس',`${p.id} • ${code}`);workbench()});$('#submitReview')?.addEventListener('click',()=>{if(!evidenceReady(p))return showToast('پرونده ناقص است','همه هشت شاخص باید امتیاز، وضعیت و حداقل یک مستند داشته باشند.');if(!weightsReady())return showToast('ضرایب ناقص است','جمع وزن شاخص‌ها باید دقیقاً ۱۰۰٪ باشد.');p.status='در انتظار کنترل';p.submittedAt=nowFa();persist('ارسال برای کنترل مسئول ارزیابی',p.id);workbench();showToast('ارسال شد','پرونده در صف کنترل مسئول ارزیابی قرار گرفت.')});$('#finalizeEvaluation')?.addEventListener('click',()=>{const c=caregiver();if(p.status!=='در انتظار کنترل')return showToast('ترتیب فرآیند رعایت نشده','ابتدا پرونده را برای کنترل ارسال کنید.');const score=qScore(p);if(score===null)return showToast('محاسبه ممکن نیست','مستندات یا ضرایب ناقص هستند.');if(!c.rank?.code||!c.rank?.decisionRef)return showToast('رتبه ثبت نشده','قبل از نهایی‌سازی، رتبه و مرجع تصمیم را در بخش رتبه‌بندی ثبت کنید.');if(!c.license?.decisionRef)return showToast('پروانه تعیین تکلیف نشده','وضعیت پروانه و مرجع تصمیم را ثبت کنید.');p.status='نهایی';p.finalScore=score;p.finalizedAt=nowFa();persist('نهایی‌سازی کارنامه حرفه‌ای',`${p.id} • امتیاز ${score}`);workbench();showToast('کارنامه نهایی شد','نتیجه رسمی در پنل مراقب قابل مشاهده و قابل اعتراض است.')})}
function bindEvents(){$('#eventForm')?.addEventListener('submit',e=>{e.preventDefault();const f=new FormData(e.currentTarget),[type,code]=String(f.get('catalog')).split('|'),cat=[...P,...N].find(x=>x.code===code),contract=String(f.get('contract')).trim(),date=String(f.get('date')).trim(),evidence=String(f.get('evidence')).trim();const dup=state.events.some(x=>x.caregiverId===caregiver().id&&x.code===code&&x.contract===contract&&x.date===date&&x.evidence===evidence);if(dup)return showToast('رویداد تکراری است','برای این کد، قرارداد، تاریخ و مستند قبلاً رکورد ثبت شده است.');state.events.push({id:uid(type),periodId:period().id,caregiverId:caregiver().id,type,code,title:cat.title,points:cat.points,contract,date,unit:String(f.get('unit')),evidence,status:'پیشنهادی',createdAt:nowFa()});persist('ثبت رویداد امتیازی پیشنهادی',`${code} • ${contract}`);eventsPage();showToast('ثبت شد','رویداد برای بررسی و تأیید آماده است.')});$$('[data-approve-event]').forEach(b=>b.onclick=()=>{const x=state.events.find(e=>e.id===b.dataset.approveEvent);x.status='تأییدشده';x.approvedAt=nowFa();x.approvedBy=currentActor();persist('تأیید رویداد امتیازی',`${x.code} • ${x.id}`);eventsPage()});$$('[data-reject-event]').forEach(b=>b.onclick=()=>{const x=state.events.find(e=>e.id===b.dataset.rejectEvent);x.status='ردشده';persist('رد رویداد امتیازی',`${x.code} • ${x.id}`);eventsPage()});$$('[data-open-period]').forEach(b=>b.onclick=()=>{ui.periodId=b.dataset.openPeriod;localStorage.setItem(UI_KEY,JSON.stringify(ui));workbench()})}
function bindComplaints(){$('#complaintForm')?.addEventListener('submit',e=>{e.preventDefault();const f=new FormData(e.currentTarget),x={id:uid('C'),caregiverId:caregiver().id,contract:String(f.get('contract')),complainant:String(f.get('complainant')),occurredAt:String(f.get('occurredAt')),location:String(f.get('location')),urgency:String(f.get('urgency')),description:String(f.get('description')),evidence:String(f.get('evidence')),status:'ثبت‌شده',screening:'',severity:'',result:'',action:'',defense:'',review:'',step:1,createdAt:nowFa()};state.complaints.push(x);ui.complaintId=x.id;persist('ثبت شکایت رسمی',x.id);complaintsPage();showToast('شکایت ثبت شد',`کد رهگیری ${x.id}`)});$$('[data-case]').forEach(b=>b.onclick=()=>{ui.complaintId=b.dataset.case;localStorage.setItem(UI_KEY,JSON.stringify(ui));complaintsPage()});$('#caseUpdateForm')?.addEventListener('submit',e=>{e.preventDefault();const x=state.complaints.find(c=>c.id===ui.complaintId),f=new FormData(e.currentTarget);x.screening=String(f.get('screening'));x.severity=String(f.get('severity'));x.result=String(f.get('result'));x.action=String(f.get('action'));x.defense=String(f.get('defense'));x.review=String(f.get('review'));x.step=x.result?7:x.defense?5:x.screening?3:2;x.status=x.result?'مختومه':'در حال رسیدگی';if(['S-3','S-4'].includes(x.severity)||['C-14','C-15'].includes(x.result))x.status='ارجاع به کمیته';persist('به‌روزرسانی پرونده شکایت',`${x.id} • ${x.status}`);complaintsPage();showToast('پرونده به‌روزرسانی شد','تمام مراحل در سابقه حسابرسی ثبت شد.')})}
function bindTraining(){$('#trainingForm')?.addEventListener('submit',e=>{e.preventDefault();const f=new FormData(e.currentTarget),cat=CPD.find(x=>x.code===f.get('code'));state.training.push({id:uid('T'),caregiverId:caregiver().id,code:cat.code,title:String(f.get('title')),date:String(f.get('date')),score:String(f.get('score')),attendance:Number(f.get('attendance')),evidence:String(f.get('evidence')),credit:cat.credit,status:'پیشنهادی',createdAt:nowFa()});persist('ثبت فعالیت آموزشی پیشنهادی',cat.code);trainingPage();showToast('فعالیت ثبت شد','در انتظار تأیید واحد آموزش است.')});$$('[data-approve-training]').forEach(b=>b.onclick=()=>{const x=state.training.find(t=>t.id===b.dataset.approveTraining);x.status='تأییدشده';x.approvedAt=nowFa();x.approvedBy=currentActor();persist('تأیید اعتبار آموزشی',`${x.id} • ${x.credit}`);trainingPage()})}
function bindRank(){$('#rankForm')?.addEventListener('submit',e=>{e.preventDefault();const f=new FormData(e.currentTarget),r=RANKS.find(x=>x.code===f.get('code'));if(!r)return showToast('رتبه انتخاب نشده','یکی از رتبه‌های مصوب R-1 تا R-5 را انتخاب کنید.');const c=caregiver();c.rank={code:r.code,title:r.title,stars:r.stars,pri:Number(f.get('pri')),validFrom:String(f.get('validFrom')),validTo:String(f.get('validTo')),decisionRef:String(f.get('decisionRef')),updatedAt:nowFa()};persist('ثبت رتبه حرفه‌ای',`${c.id} • ${r.code} • ${c.rank.decisionRef}`);rankLicensePage();showToast('رتبه ثبت شد','تاریخچه و مرجع تصمیم در پرونده حرفه‌ای ذخیره شد.')});$('#licenseForm')?.addEventListener('submit',e=>{e.preventDefault();const f=new FormData(e.currentTarget),c=caregiver();c.license={number:String(f.get('number')),status:String(f.get('status')),issuedAt:String(f.get('issuedAt')),expiresAt:String(f.get('expiresAt')),decisionRef:String(f.get('decisionRef')),updatedAt:nowFa()};persist('ثبت وضعیت پروانه صلاحیت',`${c.id} • ${c.license.status}`);rankLicensePage();showToast('پروانه به‌روزرسانی شد','وضعیت جدید در کارنامه مراقب نمایش داده می‌شود.')})}
function bindAppeals(){$('#appealForm')?.addEventListener('submit',e=>{e.preventDefault();const f=new FormData(e.currentTarget),x={id:uid('AP'),caregiverId:caregiver().id,periodId:String(f.get('periodId')),subject:String(f.get('subject')),description:String(f.get('description')),evidence:String(f.get('evidence')),status:'ثبت‌شده',createdAt:nowFa(),submittedBy:currentActor(),decision:'',decisionRef:''};state.appeals.push(x);persist('ثبت درخواست بازبینی',x.id);appealsPage(currentActor()==='مراقب');showToast('اعتراض ثبت شد',`کد رهگیری ${x.id}`)});$('#appealDecisionForm')?.addEventListener('submit',e=>{e.preventDefault();const f=new FormData(e.currentTarget),x=state.appeals.find(a=>a.id===f.get('id'));x.status=String(f.get('status'));x.decision=String(f.get('decision'));x.decisionRef=String(f.get('decisionRef'));x.reviewedAt=nowFa();x.reviewer=currentActor();persist('ثبت نتیجه بازبینی',`${x.id} • ${x.status}`);appealsPage(false);showToast('نتیجه ثبت شد','در صورت پذیرش، خروجی‌های وابسته باید توسط مسئول ارزیابی بازتولید شوند.')})}
function bindQuality(){$('#policyForm')?.addEventListener('submit',e=>{e.preventDefault();const f=new FormData(e.currentTarget),weights=Object.fromEntries(Q.map(x=>[x.code,f.get(x.code)===''?null:Number(f.get(x.code))])),sum=Object.values(weights).reduce((a,b)=>a+Number(b||0),0);if(Math.round(sum*100)/100!==100)return showToast('جمع ضرایب نامعتبر است',`جمع فعلی ${fa(sum)}٪ است و باید دقیقاً ۱۰۰٪ باشد.`);state.policy.weights=weights;state.policy.minPerformance=f.get('minPerformance')===''?null:Number(f.get('minPerformance'));state.policy.periodType=String(f.get('periodType'));state.policy.approvedBy=String(f.get('approvedBy'));state.policy.approvedAt=String(f.get('approvedAt'));persist('ثبت تنظیمات مصوب موتور ارزیابی',`${POLICY_VERSION} • ${state.policy.approvedBy}`);qualityPage();showToast('تنظیمات ثبت شد','محاسبات بعدی با نسخه و ضرایب جدید انجام می‌شود.')});$('#exportAudit')?.addEventListener('click',()=>{const blob=new Blob([JSON.stringify(state.audit,null,2)],{type:'application/json'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='salamat-aval-evaluation-audit.json';a.click();URL.revokeObjectURL(a.href)})}
function patchRoles(){const care=roles.caregiver;care.nav=care.nav.map(n=>n[1]==='کارنامه کاری'?[n[0],'کارنامه حرفه‌ای',n[2]]:n[1]==='درجه و رتبه'?[n[0],'رتبه و پروانه',n[2]]:n);if(!care.nav.some(n=>n[1]==='اعتراضات و بازبینی'))care.nav.push(['message','اعتراضات و بازبینی']);const hr=roles.hr;const hrExtra=[['chart','میزکار ارزیابی'],['account','پرونده حرفه‌ای مراقبین'],['calendar','دوره‌های ارزیابی'],['badge','امتیازات P و N'],['alert','شکایات و تخلفات'],['book','بازآموزی و CPD'],['shield','رتبه‌بندی و پروانه'],['message','اعتراضات'],['settings','کنترل کیفیت ارزیابی']];hrExtra.forEach(n=>{if(!hr.nav.some(x=>x[1]===n[1]))hr.nav.push(n)});['recruiter','admin'].forEach(k=>{if(!roles[k].nav.some(x=>x[1]==='پرونده حرفه‌ای مراقبین'))roles[k].nav.push(['chart','پرونده حرفه‌ای مراقبین'])})}
function boot(){if(typeof roles==='undefined'||typeof renderDashboard!=='function'||typeof renderModule!=='function')return setTimeout(boot,80);patchRoles();const previousDashboard=renderDashboard,previousModule=renderModule;renderDashboard=function(r){if(r===roles.caregiver||selectedRole==='caregiver')return caregiverDashboard();if(r===roles.hr||selectedRole==='hr')return hrDashboard();return previousDashboard(r)};renderModule=function(r,n){const label=n?.[1]||'';if((r===roles.caregiver||selectedRole==='caregiver')&&['کارنامه حرفه‌ای','رتبه و پروانه','اعتراضات و بازبینی'].includes(label))return renderEvalModule(label);if((r===roles.hr||selectedRole==='hr')&&['میزکار ارزیابی','پرونده حرفه‌ای مراقبین','دوره‌های ارزیابی','امتیازات P و N','شکایات و تخلفات','بازآموزی و CPD','رتبه‌بندی و پروانه','اعتراضات','کنترل کیفیت ارزیابی'].includes(label))return renderEvalModule(label);if((r===roles.recruiter||r===roles.admin||['recruiter','admin'].includes(selectedRole))&&label==='پرونده حرفه‌ای مراقبین')return readonlyFilePage();return previousModule(r,n)};if(!$('#appView').classList.contains('hidden')){if(selectedRole==='caregiver')caregiverDashboard();else if(selectedRole==='hr')hrDashboard()}}
boot();
})();
