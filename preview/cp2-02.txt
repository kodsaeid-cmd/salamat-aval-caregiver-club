</h3></div></div><div class="cp2-contract-list">${contracts.map(x=>`<div class="cp2-contract-row ${x.status==='فعال'?'active':''}"><div><strong>${x.id} • ${x.family}</strong><small>${x.service}</small></div><div><strong>${x.start}</strong><small>شروع</small></div><div><strong>${x.end}</strong><small>پایان</small></div><span class="status">${x.status}</span></div>`).join('')}</div></article>`}
function edu(){let formats=['ویدئو','PDF','صوت'];let items=s.courses.map((x,i)=>({...x,format:formats[i%3],assignedBy:i%2?'منابع انسانی':'کارشناس جذب',assignedAt:['۲۵ تیر','۲۶ تیر','۲۸ تیر'][i%3]}));return `<section class="cp-page-head"><div><span class="cp-eyebrow">بانک آموزش تخصیص‌یافته</span><h2>آموزش‌های من</h2><p>فایل‌های ارسال‌شده از پنل جذب و منابع انسانی به ترتیب تخصیص نمایش داده می‌شوند.</p></div></section><div class="cp2-learning-toolbar"><button class="active" data-f="all">همه</button><button data-f="ویدئو">ویدئو</button><button data-f="PDF">PDF</button><button data-f="صوت">صوت</button></div><section class="cp2-learning-list" id="learn">${items.map((x,i)=>`<article class="cp2-learning-row" data-format="${x.format}"><div class="cp2-file-icon">${x.format}</div><div><h3>${fa(i+1)}. ${x.title}</h3><p>ارسال توسط ${x.assignedBy} • ${x.assignedAt} ۱۴۰۵</p></div><div><small>${x.duration} • ${fa(x.progress)}٪</small>${prog(x.progress)}</div><button class="btn ${x.progress?'outline':'primary'}">${x.progress?'ادامه':'شروع'}</button></article>`).join('')}</section>`}
let selected=contracts[0].id,tickets={};function support(){let c=contracts.find(x=>x.id===selected),msgs=tickets[selected]||s.supportMessages||[];return `<section class="cp-page-head"><div><span class="cp-eyebrow">پشتیبانی مبتنی بر قرارداد</span><h2>پشتیبانی قراردادها</h2><p>با انتخاب هر قرارداد، گزارش و پیام همان قرارداد دیده می‌شود.</p></div></section><section class="cp2-support-layout"><aside class="surface"><div class="surface-head"><div><h3>قراردادها</h3></div></div><div class="cp2-contract-selector">${contracts.map(x=>`<button class="${x.id===selected?'active':''}" data-contract="${x.id}"><strong>${x.id} • ${x.family}</strong><small>${x.start} تا ${x.end}</small></button>`).join('')}</div></aside><article class="surface cp-chat"><div class="surface-head"><div><h3>${c.id} • ${c.family}</h3><p>پشتیبان: ${s.profile.supportAgent}</p></div><button class="btn primary" id="newTicket">گزارش جدید</button></div>${msgs.length?`<div class="cp-chat-messages">${msgs.map(m=>`<div class="cp-message ${m.from==='caregiver'?'mine':''}"><p>${m.text}</p><small>${m.date}</small></div>`).join('')}</div>`:empty('message','پیامی برای این قرارداد نیست','گزارش یا پیام جدید ثبت کنید.')}<form class="cp-chat-form" id="support2"><input id="supportText" required placeholder="پیام مرتبط با قرارداد..."><button class="btn primary">ارسال</button></form></article></section>`}
function sec(){return `<section class="cp-security-hero"><span data-icon="shield"></span><div><small>لاین محرمانه</small><h2>گزارش امنیت</h2><p>دو امکان مستقل تماس امن و پیام به پشتیبان امن.</p></div></section><section class="cp2-security-actions"><article class="cp2-sec-action"><span data-icon="phone"></span><h3>تماس امن با پشتیبان</h3><p>برای موقعیت فوری یا نیاز به گفت‌وگوی مستقیم.</p><a href="tel:1527" class="btn primary">تماس امن با ۱۵۲۷</a></article><article class="cp2-sec-action"><span data-icon="message"></span><h3>پیام به پشتیبان امن</h3><p>پیام فقط برای افراد مجاز امنیتی قابل مشاهده است.</p><button class="btn outline" id="secureMsg">ثبت پیام امن</button></article></section>`}
function cal(){let c=contracts.find(x=>x.status==='فعال'),days=[],off=4;for(let i=0;i<off;i++)days.push({n:28+i,m:1});for(let d=1;d<=31;d++)days.push({n:d,w:c?.days.includes(d),t:d===7});while(days.length%7)days.push({n:days.length-34,m:1});return `<section class="cp-page-head"><div><span class="cp-eyebrow">تقویم شمسی قرارداد</span><h2>مرداد ۱۴۰۵</h2><p>روزهای کاری از قرارداد فعال خوانده می‌شوند.</p></div><button class="btn primary" id="leave2">درخواست مرخصی</button></section><section class="cp2-calendar-wrap"><article class="surface cp2-calendar"><div class="cp2-calendar-head"><button>‹</button><h3>مرداد ۱۴۰۵</h3><button>›</button></div><div class="cp2-weekdays">${['شنبه','یکشنبه','دوشنبه','سه‌شنبه','چهارشنبه','پنجشنبه','جمعه'].map(x=>`<span>${x}</span>`).join('')}</div><div class="cp2-days">${days.map(d=>`<div class="cp2-day ${d.m?'muted':''} ${d.w?'work':''} ${d.t?'today':''}"><strong>${fa(d.n)}</strong>${d.w?'<i></i>':''}<small>${d.w?c.family+'<br>شیفت قراردادی':''}</small></div>`).join('')}</div></article><aside class="surface cp2-calendar-side"><h3>قرارداد مبنا</h3><p>${c.id}<br>${c.family}<br>${c.start} تا ${c.end}</p><div class="cp2-legend"><div><i></i>روز کاری قرارداد</div><div><i></i>مرخصی</div></div></aside></section>`}
function page(l){return l==='کارنامه کاری'?perf():l==='درجه و رتبه'?rank():l==='کیف پول'?wallet():l==='حقوق و فیش حقوقی'?slips():l==='ساعات قرارداد'?cont():l==='آموزش‌های من'?edu():l.includes('پشتیبانی')?support():l==='گزارش امنیت'?sec():cal()}
function go(l){qa('.nav-item').find(x=>x.textContent.includes(l))?.click()}function bind(){qa('[data-go]').forEach(b=>b.onclick=()=>go(b.dataset.go));qa('[data-contract]').forEach(b=>b.onclick=()=>{selected=b.dataset.contract;set('پشتیبانی قراردادها',support())});qa('[data-f]').forEach(b=>b.onclick=()=>{qa('[data-f]').forEach(x=>x.classList.remove('active'));b.classList.add('active');qa('#learn [data-format]').forEach(r=>r.style.display=b.dataset.f==='all'||r.dataset.format===b.dataset.f?'grid':'none')});q('#support2')?.addEventListener('submit',e=>{e.preventDefault();(tickets[selected]||(tickets[selected]=[])).push({from:'caregiver',text:q('#supportText').value,date:'اکنون'});set('پشتیبانی قراردادها',support());toast('پیام ارسال شد','پیام به قرارداد انتخابی افزوده شد.')});q('#secureMsg')?.addEventListener('click',()=>{q('#drawerTitle').textContent='پیام امن';q('#drawerBody').innerHTML='<form class="cp-form" id="sm"><label class="cp-full">موضوع<input required></label><label class="cp-full">شرح<textarea rows="6" required></textarea></label><button class="btn primary cp-full">ارسال امن</button></form>';q('#drawer').classList.add('open');q('#drawerBackdrop').classList.remove('hidden');q('#sm').onsubmit=e=>{e.preventDefault();closeDrawer();toast('پیام امن ثبت شد','فقط پشتیبان امن آن را می‌بیند.')}});q('#leave2')?.addEventListener('click',()=>toast('درخواست مرخصی','فرم مرخصی در اتصال به منابع انسانی فعال می‌شود.'));q('#payReq')?.addEventListener('click',()=>toast('درخواست دریافت','درخواست برای واحد مالی ثبت می‌شود.'))}
renderDashboard=function(r){if(r===roles.caregiver||selectedRole==='caregiver')return dash();return oldD(r)};renderModule=function(r,n){if(r===roles.caregiver||selectedRole==='caregiver')return set(n[1],page(n[1]));return oldM(r,n)};
})();
