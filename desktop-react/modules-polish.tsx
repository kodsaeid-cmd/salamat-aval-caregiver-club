import React,{FormEvent,useEffect,useMemo,useState} from "react";
import {BadgeCheck,BookOpen,ClipboardCheck,FilePenLine,Search,Trash2,Upload} from "lucide-react";
import {api,can,Card,dateFa,Empty,ErrorState,fa,initials,Loading,Metric,Modal,Notify,status,text,uploadFile} from "./core";

const caregiverIdOf=(value:any)=>String(value?.id||value?.caregiverId||value?.caregiver_id||value?.profileId||"").trim();
const listFromCaregiverPayload=(payload:any)=>{const d=payload?.data??payload??{};if(Array.isArray(d))return d;if(Array.isArray(d.items))return d.items;if(Array.isArray(d.caregivers))return d.caregivers;if(Array.isArray(d.rows))return d.rows;return []};
const paginationFromCaregiverPayload=(payload:any)=>{const d=payload?.data??payload??{};return d.pagination||payload?.pagination||{}};

function CaregiverEdit({id,onDone,notify}:{id:string;onDone:()=>void;notify:Notify}){
 const [item,setItem]=useState<any>(null),[busy,setBusy]=useState(false),[error,setError]=useState("");
 const load=async()=>{setError("");try{const p:any=await api(`/api/admin/caregiver-profile?id=${encodeURIComponent(id)}`);setItem(p.data||{})}catch(e:any){setError(e.message)}};
 useEffect(()=>{void load()},[id]);
 if(error)return <ErrorState message={error} retry={load}/>;
 if(!item)return <Loading label="در حال دریافت پرونده مراقب..."/>;
 const submit=async(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();const fd=new FormData(e.currentTarget),data:any=Object.fromEntries(fd.entries());data.caregiverId=id;for(const n of ["blacklisted","active","profileCompleted"])data[n]=fd.get(n)==="on";data.acceptedShifts=String(data.shiftServices||"").split(/[,،]/).map(x=>x.trim()).filter(Boolean);setBusy(true);try{await api("/api/admin/caregiver-profile",{method:"PATCH",body:JSON.stringify(data)});notify("پرونده مراقب به‌روزرسانی شد.","success");onDone()}catch(x:any){notify(x.message,"error")}finally{setBusy(false)}};
 const uploadAvatar=async(file?:File)=>{if(!file)return;try{const params=new URLSearchParams({caregiverId:id});if(item.userId)params.set("userId",item.userId);const r=await fetch(`/api/profile-images?${params}`,{method:"POST",headers:{"content-type":file.type,"x-file-size":String(file.size)},body:file,credentials:"same-origin"});const p:any=await r.json().catch(()=>({}));if(!r.ok)throw new Error(p.message||"بارگذاری تصویر انجام نشد");notify("تصویر پروفایل به‌روزرسانی شد.","success");await load()}catch(x:any){notify(x.message,"error")}};
 return <form className="da-form" onSubmit={submit}>
  <div className="da-form-section"><h3>اتصال CRM و حساب</h3><div className="da-form-grid">
   <label><span>شناسه CRM</span><input name="crmRecordId" defaultValue={item.crmRecordId||""}/></label><label><span>شماره پرونده</span><input name="membershipCode" defaultValue={item.membershipCode||""}/></label><label><span>نام کاربری</span><input name="username" defaultValue={item.username||""}/></label><label><span>وضعیت حساب</span><select name="accountStatus" defaultValue={item.accountStatus||"ACTIVE"}><option value="ACTIVE">فعال</option><option value="PENDING">در انتظار</option><option value="SUSPENDED">تعلیق</option><option value="INACTIVE">غیرفعال</option></select></label><label><span>رمز جدید</span><input name="password" type="password" minLength={8}/></label><label><span>تصویر پروفایل</span><input type="file" accept="image/jpeg,image/png,image/webp" onChange={e=>void uploadAvatar(e.target.files?.[0])}/></label>
  </div></div>
  <div className="da-form-section"><h3>هویت و تماس</h3><div className="da-form-grid">
   <label><span>نام</span><input name="firstName" defaultValue={item.firstName||""}/></label><label><span>نام خانوادگی</span><input name="lastName" defaultValue={item.lastName||""}/></label><label><span>نام کامل</span><input name="fullName" defaultValue={item.fullName||""} required/></label><label><span>نام پدر</span><input name="fatherName" defaultValue={item.fatherName||""}/></label><label><span>کد ملی</span><input name="nationalId" defaultValue={item.nationalId||""}/></label><label><span>موبایل</span><input name="mobile" defaultValue={item.mobile||""}/></label><label><span>تلفن ثابت</span><input name="landline" defaultValue={item.landline||""}/></label><label><span>تاریخ تولد</span><input name="birthDate" defaultValue={item.birthDate||""}/></label><label><span>جنسیت</span><input name="gender" defaultValue={item.gender||""}/></label><label><span>سن</span><input name="age" type="number" defaultValue={item.age||""}/></label><label><span>لهجه</span><input name="dialect" defaultValue={item.dialect||""}/></label>
  </div></div>
  <div className="da-form-section"><h3>حرفه و عملیات</h3><div className="da-form-grid">
   <label><span>تخصص</span><input name="specialty" defaultValue={item.specialty||""}/></label><label><span>نوع اصلی</span><input name="primaryType" defaultValue={item.primaryType||""}/></label><label><span>محدوده منزل</span><input name="homeRegion" defaultValue={item.homeRegion||""}/></label><label><span>محدوده فعالیت</span><input name="activityRegion" defaultValue={item.activityRegion||""}/></label><label className="wide"><span>شیفت‌ها و خدمات</span><input name="shiftServices" defaultValue={Array.isArray(item.acceptedShifts)?item.acceptedShifts.join("، "):item.shiftServices||""}/></label><label><span>وضعیت پرونده</span><input name="fileStatus" defaultValue={item.fileStatus||""}/></label><label><span>تکمیل مدارک</span><input name="documentsCompleted" defaultValue={item.documentsCompleted||""}/></label><label><span>تحویل مدارک</span><input name="documentsDelivered" defaultValue={item.documentsDelivered||""}/></label><label><span>مشغول به کار</span><input name="employed" defaultValue={item.employed||""}/></label><label className="wide"><span>سوابق و توضیحات</span><textarea name="workHistory" defaultValue={item.workHistory||""}/></label><label className="check"><input type="checkbox" name="blacklisted" defaultChecked={Boolean(item.blacklisted)}/>لیست سیاه</label><label className="check"><input type="checkbox" name="active" defaultChecked={Boolean(item.active)}/>پرونده فعال</label><label className="check"><input type="checkbox" name="profileCompleted" defaultChecked={Boolean(item.profileCompleted)}/>پروفایل کامل</label>
  </div></div>
  <button className="da-btn primary" disabled={busy}>{busy?"در حال ذخیره...":"ذخیره پرونده"}</button>
 </form>
}

export function CaregiversPage({access,notify}:{access:any;notify:Notify}){
 const [rows,setRows]=useState<any[]>([]),[page,setPage]=useState(1),[pagination,setPagination]=useState<any>({}),[query,setQuery]=useState(""),[loading,setLoading]=useState(true),[error,setError]=useState(""),[selected,setSelected]=useState<any>(null),[detailLoading,setDetailLoading]=useState(false),[edit,setEdit]=useState("");
 const load=async(next=page,q=query)=>{setLoading(true);setError("");try{const params=new URLSearchParams({page:String(next)});if(q.trim())params.set("q",q.trim());const p:any=await api(`/api/admin/caregivers-page?${params}`);setRows(listFromCaregiverPayload(p));setPagination(paginationFromCaregiverPayload(p));setPage(next)}catch(e:any){setRows([]);setError(e.message)}finally{setLoading(false)}};
 useEffect(()=>{void load(1,"")},[]);
 const open=async(item:any)=>{const id=caregiverIdOf(item);if(!id)return notify("شناسه پرونده مراقب در پاسخ سرور موجود نیست.","error");setSelected(item);setDetailLoading(true);const [record,profile]=await Promise.allSettled([api<any>(`/api/admin/caregiver-record?id=${encodeURIComponent(id)}`),api<any>(`/api/admin/caregiver-profile?id=${encodeURIComponent(id)}`)]);const recordData=record.status==="fulfilled"?(record.value.data||{}):{},profileData=profile.status==="fulfilled"?(profile.value.data||{}):{};setSelected({...item,...recordData,...profileData,id});setDetailLoading(false);if(record.status==="rejected"&&profile.status==="rejected")notify("جزئیات پرونده از سرور دریافت نشد؛ اطلاعات فهرست نمایش داده می‌شود.","error")};
 const act=async(item:any,action:"ACTIVE"|"SUSPENDED"|"DELETE")=>{const id=caregiverIdOf(item);if(!id)return;if(action==="DELETE"&&!confirm(`پرونده «${item.fullName||"مراقب"}» حذف شود؟`))return;try{if(action==="DELETE")await api(`/api/admin/caregivers/${encodeURIComponent(id)}`,{method:"DELETE"});else await api(`/api/admin/caregivers/${encodeURIComponent(id)}/status`,{method:"PATCH",body:JSON.stringify({status:action})});notify(action==="ACTIVE"?"مراقب تأیید شد.":action==="SUSPENDED"?"مراقب تعلیق شد.":"مراقب حذف شد.","success");setSelected(null);await load(page,query)}catch(x:any){notify(x.message,"error")}};
 const totalPages=Number(pagination.totalPages||pagination.pages||1),total=Number(pagination.total||rows.length);
 return <div className="da-stack"><Card><div className="da-toolbar"><form onSubmit={e=>{e.preventDefault();void load(1,query)}}><Search size={18}/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="نام، شماره پرونده، موبایل یا کد ملی"/><button>جست‌وجو</button></form></div></Card>{error?<ErrorState message={error} retry={()=>load()}/>:loading?<Loading label="در حال دریافت پرونده‌های مراقبین..."/>:<div className="da-master-detail"><Card><div className="da-mini-list">{rows.length?rows.map((c,i)=>{const id=caregiverIdOf(c)||String(i);return <button key={id} className={caregiverIdOf(selected)===caregiverIdOf(c)?"active":""} onClick={()=>void open(c)}><span>{initials(c.fullName)}</span><div><strong>{text(c.fullName,"مراقب")}</strong><small>{text(c.membershipCode)} • {text(c.mobile)}</small></div><em>{status(c.accountStatus||c.status||c.cooperationStatus)}</em></button>}):<Empty title="پرونده‌ای پیدا نشد" description="عبارت جست‌وجو یا فیلتر را تغییر دهید."/>}</div><div className="da-pager"><button disabled={page<=1} onClick={()=>load(page-1)}>قبلی</button><span>{fa(total)} پرونده</span><button disabled={page>=totalPages} onClick={()=>load(page+1)}>بعدی</button></div></Card><div>{selected?<Card>{detailLoading?<Loading label="در حال بازکردن پرونده..."/>:<><div className="da-profile-head"><div className="da-big-avatar">{initials(selected.fullName)}</div><div><h2>{text(selected.fullName,"مراقب")}</h2><p>{text(selected.membershipCode)} • {text(selected.mobile)} • {text(selected.specialty||selected.primaryType)}</p></div><span className="da-pill">{status(selected.accountStatus||selected.status||selected.cooperationStatus)}</span></div><div className="da-detail-grid"><div><span>کد ملی</span><strong>{text(selected.nationalId)}</strong></div><div><span>CRM</span><strong>{text(selected.crmRecordId)}</strong></div><div><span>محدوده فعالیت</span><strong>{text(selected.activityRegion)}</strong></div><div><span>پروفایل</span><strong>{selected.profileCompleted?"تکمیل":"ناقص"}</strong></div><div><span>لیست سیاه</span><strong>{selected.blacklisted?"بله":"خیر"}</strong></div><div><span>آخرین ورود</span><strong>{dateFa(selected.lastLoginAt)}</strong></div></div><div className="da-actions">{can(access,"staff.caregivers","update")&&<><button className="da-btn primary" onClick={()=>setEdit(caregiverIdOf(selected))}><FilePenLine size={17}/>ویرایش پرونده</button><button className="da-btn soft" onClick={()=>void act(selected,"ACTIVE")}><BadgeCheck size={17}/>تأیید/فعال</button><button className="da-btn warn" onClick={()=>void act(selected,"SUSPENDED")}>تعلیق</button></>}{can(access,"staff.caregivers","delete")&&<button className="da-btn danger" onClick={()=>void act(selected,"DELETE")}><Trash2 size={17}/>حذف</button>}</div></>}</Card>:<Empty title="مراقب را انتخاب کنید" description="اطلاعات پرونده و عملیات مدیریتی در این بخش نمایش داده می‌شود."/>}</div></div>}{edit&&<Modal title="ویرایش کامل پرونده مراقب" subtitle="اطلاعات مستقیماً در دیتابیس باشگاه ذخیره می‌شود." onClose={()=>setEdit("")} wide><CaregiverEdit id={edit} notify={notify} onDone={()=>{setEdit("");void load(page,query);if(selected)void open(selected)}}/></Modal>}</div>
}

export function TrainingPage({access,notify}:{access:any;notify:Notify}){
 const [data,setData]=useState<any>(null),[error,setError]=useState(""),[create,setCreate]=useState(false),[assign,setAssign]=useState(false),[caregivers,setCaregivers]=useState<any[]>([]),[recipientQuery,setRecipientQuery]=useState(""),[recipientLoading,setRecipientLoading]=useState(false),[recipientError,setRecipientError]=useState(""),[selectedRecipients,setSelectedRecipients]=useState<Set<string>>(new Set());
 const load=async()=>{setError("");try{const a:any=await api("/api/training/admin");setData(a.data||{})}catch(e:any){setError(e.message)}};
 useEffect(()=>{void load()},[]);
 const searchRecipients=async(q=recipientQuery)=>{setRecipientLoading(true);setRecipientError("");try{const params=new URLSearchParams({page:"1",pageSize:"80"});if(q.trim())params.set("q",q.trim());const p:any=await api(`/api/admin/caregivers-page?${params}`);setCaregivers(listFromCaregiverPayload(p))}catch(e:any){setCaregivers([]);setRecipientError(e.message)}finally{setRecipientLoading(false)}};
 const openAssign=()=>{setAssign(true);setRecipientQuery("");setSelectedRecipients(new Set());void searchRecipients("")};
 const toggleRecipient=(id:string,checked:boolean)=>setSelectedRecipients(prev=>{const next=new Set(prev);if(checked)next.add(id);else next.delete(id);return next});
 const createCourse=async(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();const fd=new FormData(e.currentTarget);try{let contentUrl=String(fd.get("contentUrl")||"").trim();const file=fd.get("file");if(file instanceof File&&file.size){const stored=await uploadFile(file,"training");contentUrl=stored.downloadUrl||stored.url||`/api/files/${stored.id}/download`}await api("/api/training/courses",{method:"POST",body:JSON.stringify({title:fd.get("title"),category:fd.get("category"),description:fd.get("description"),durationMinutes:Number(fd.get("durationMinutes")||0),credit:Number(fd.get("credit")||0),mandatory:fd.get("mandatory")==="on",contentUrl})});notify("آموزش در بانک سازمان ثبت شد.","success");setCreate(false);await load()}catch(x:any){notify(x.message,"error")}};
 const assignCourse=async(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();const fd=new FormData(e.currentTarget),ids=[...selectedRecipients];if(!ids.length)return notify("حداقل یک مراقب را انتخاب کنید.","error");try{await api("/api/training/assignments",{method:"POST",body:JSON.stringify({courseId:fd.get("courseId"),caregiverIds:ids,dueAt:fd.get("dueAt"),assignmentNote:fd.get("assignmentNote")})});notify(`آموزش برای ${fa(ids.length)} مراقب ارسال شد.`,"success");setAssign(false);setSelectedRecipients(new Set());await load()}catch(x:any){notify(x.message,"error")}};
 if(error)return <ErrorState message={error} retry={load}/>;if(!data)return <Loading/>;const courses=data.courses||[],assignments=data.assignments||[];
 return <div className="da-stack"><section className="da-metrics"><Metric label="آموزش‌ها" value={fa(courses.length)}/><Metric label="تخصیص‌ها" value={fa(assignments.length)}/><Metric label="دفعات بازشدن" value={fa(courses.reduce((s:number,x:any)=>s+Number(x.totalOpenCount||0),0))}/><Metric label="زمان مشاهده" value={`${fa(Math.round(courses.reduce((s:number,x:any)=>s+Number(x.totalViewSeconds||0),0)/60))} دقیقه`}/></section><div className="da-actions">{can(access,"staff.training","create")&&<><button className="da-btn primary" onClick={()=>setCreate(true)}><BookOpen size={17}/>ثبت آموزش</button><button className="da-btn soft" onClick={openAssign}><Upload size={17}/>ارسال به مراقب</button></>}</div><Card><div className="da-table-wrap"><table className="da-table"><thead><tr><th>آموزش</th><th>دسته</th><th>تخصیص</th><th>بازدید</th><th>زمان</th><th>وضعیت</th></tr></thead><tbody>{courses.map((x:any)=><tr key={x.id}><td><strong>{x.title}</strong><small>{text(x.code)}</small></td><td>{text(x.category)}</td><td>{fa(x.assignedCount)}</td><td>{fa(x.totalOpenCount)}</td><td>{fa(Math.round(Number(x.totalViewSeconds||0)/60))} دقیقه</td><td>{status(x.status)}</td></tr>)}</tbody></table></div></Card><Card><div className="da-card-head"><h3>ریز مشاهده مراقبین</h3></div><div className="da-table-wrap"><table className="da-table"><thead><tr><th>مراقب</th><th>آموزش</th><th>ارسال‌کننده</th><th>بازدید</th><th>زمان</th><th>وضعیت</th></tr></thead><tbody>{assignments.map((x:any)=><tr key={x.enrollmentId||x.id}><td>{x.caregiverName}<small>{text(x.membershipCode)}</small></td><td>{x.title}</td><td>{text(x.assignedByName)}</td><td>{fa(x.openCount)}</td><td>{fa(Math.round(Number(x.totalViewSeconds||0)/60))} دقیقه</td><td>{status(x.status)}</td></tr>)}</tbody></table></div></Card>{create&&<Modal title="ثبت محتوای آموزشی" onClose={()=>setCreate(false)}><form className="da-form" onSubmit={createCourse}><div className="da-form-grid"><label><span>عنوان</span><input name="title" required/></label><label><span>دسته‌بندی</span><input name="category"/></label><label><span>مدت اسمی دقیقه</span><input name="durationMinutes" type="number" min="0"/></label><label><span>اعتبار آموزشی</span><input name="credit" type="number" min="0"/></label><label className="wide"><span>فایل</span><input name="file" type="file" accept=".pdf,.doc,.docx,.mp4,.webm,.mp3,.m4a,.jpg,.jpeg,.png,.webp"/></label><label className="wide"><span>یا نشانی محتوا</span><input name="contentUrl" placeholder="https://... یا /api/files/..."/></label><label className="wide"><span>توضیحات</span><textarea name="description"/></label><label className="check"><input name="mandatory" type="checkbox"/>آموزش الزامی</label></div><button className="da-btn primary">ثبت آموزش</button></form></Modal>}{assign&&<Modal title="ارسال آموزش به مراقبین" subtitle="مراقب را با نام، شماره پرونده، موبایل یا کد ملی جست‌وجو کنید. انتخاب‌ها با جست‌وجوی بعدی از بین نمی‌روند." onClose={()=>setAssign(false)} wide><form className="da-form" onSubmit={assignCourse}><div className="da-form-grid"><label><span>آموزش</span><select name="courseId" required><option value="">انتخاب آموزش</option>{courses.filter((x:any)=>x.status==="ACTIVE").map((x:any)=><option key={x.id} value={x.id}>{x.title}</option>)}</select></label><label><span>مهلت مشاهده</span><input name="dueAt" type="date"/></label><label className="wide"><span>پیام همراه</span><textarea name="assignmentNote"/></label></div><div className="da-recipient-search"><Search size={18}/><input value={recipientQuery} onChange={e=>setRecipientQuery(e.target.value)} placeholder="جست‌وجوی مراقب: نام، کد، موبایل یا کد ملی" onKeyDown={e=>{if(e.key==="Enter"){e.preventDefault();void searchRecipients()}}}/><button type="button" className="da-btn soft" onClick={()=>void searchRecipients()}>جست‌وجو</button></div><div className="da-recipient-summary"><strong>{fa(selectedRecipients.size)} مراقب انتخاب شده</strong><small>می‌توانید جست‌وجو را عوض کنید و انتخاب‌های قبلی حفظ می‌شوند.</small></div>{recipientError?<ErrorState message={recipientError} retry={()=>searchRecipients()}/>:recipientLoading?<Loading label="در حال جست‌وجوی مراقبین..."/>:<div className="da-recipient-grid">{caregivers.length?caregivers.map(c=>{const id=caregiverIdOf(c);return <label key={id}><input type="checkbox" checked={selectedRecipients.has(id)} onChange={e=>toggleRecipient(id,e.target.checked)}/><span><strong>{text(c.fullName,"مراقب")}</strong><small>{text(c.membershipCode)} • {text(c.mobile)}</small></span></label>}):<Empty title="مراقبی پیدا نشد" description="عبارت جست‌وجو را تغییر دهید."/>}</div>}<button className="da-btn primary">ارسال آموزش انتخاب‌شده</button></form></Modal>}</div>
}

function EvaluationEditor({caregiver,access,notify}:{caregiver:any;access:any;notify:Notify}){
 const caregiverId=caregiverIdOf(caregiver);const [data,setData]=useState<any>(null),[evaluationId,setEvaluationId]=useState(""),[draft,setDraft]=useState<Record<string,{score:number|null;note:string}>>({}),[busy,setBusy]=useState(false),[period,setPeriod]=useState(false),[selectedIndicator,setSelectedIndicator]=useState("");
 const load=async(id=evaluationId)=>{try{const q=new URLSearchParams({caregiverId});if(id)q.set("evaluationId",id);const p:any=await api(`/api/evaluations?${q}`),d=p.data||{};setData(d);const nextId=d.evaluation?.id||id||"";setEvaluationId(nextId);const next:any={};for(const ind of d.evaluation?.indicators||[])for(const cr of ind.criteria||[])next[cr.code]={score:cr.score??null,note:cr.note||""};setDraft(next);setSelectedIndicator(current=>(d.evaluation?.indicators||[]).some((x:any)=>x.code===current)?current:"")}catch(e:any){notify(e.message,"error")}};
 useEffect(()=>{if(caregiverId)void load("")},[caregiverId]);
 const saveIndicator=async(ind:any)=>{if(!data?.evaluation)return;const scores=(ind.criteria||[]).map((c:any)=>({criterionCode:c.code,score:Number(draft[c.code]?.score),note:String(draft[c.code]?.note||"").trim()})).filter((x:any)=>Number.isInteger(x.score)&&x.score>=1&&x.score<=5);if(!scores.length)return notify("حداقل یک معیار را امتیازدهی کنید.","error");setBusy(true);try{await api(`/api/evaluations/${encodeURIComponent(data.evaluation.id)}/indicators/${encodeURIComponent(ind.code)}`,{method:"PUT",body:JSON.stringify({scores})});notify(`شاخص ${ind.code} ذخیره شد.`,"success");await load(data.evaluation.id)}catch(e:any){notify(e.message,"error")}finally{setBusy(false)}};
 const finalize=async()=>{if(!data?.evaluation||!confirm("تمام امتیازها ذخیره و این دوره نهایی شود؟"))return;setBusy(true);try{for(const ind of data.evaluation.indicators||[]){const scores=(ind.criteria||[]).map((c:any)=>({criterionCode:c.code,score:Number(draft[c.code]?.score),note:String(draft[c.code]?.note||"").trim()}));if(scores.some((x:any)=>!Number.isInteger(x.score)||x.score<1||x.score>5))throw new Error(`شاخص ${ind.code} هنوز معیار بدون امتیاز دارد.`);await api(`/api/evaluations/${encodeURIComponent(data.evaluation.id)}/indicators/${encodeURIComponent(ind.code)}`,{method:"PUT",body:JSON.stringify({scores})})}await api(`/api/evaluations/${encodeURIComponent(data.evaluation.id)}/finalize`,{method:"POST"});notify("ارزیابی نهایی شد.","success");await load(data.evaluation.id)}catch(e:any){notify(e.message,"error")}finally{setBusy(false)}};
 const createPeriod=async(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();const fd=new FormData(e.currentTarget);setBusy(true);try{const p:any=await api("/api/evaluations",{method:"POST",body:JSON.stringify({caregiverId,title:fd.get("title"),startDate:fd.get("startDate"),endDate:fd.get("endDate")})});notify("دوره ارزیابی ایجاد شد.","success");setPeriod(false);await load(p.data?.evaluation?.id||"")}catch(x:any){notify(x.message,"error")}finally{setBusy(false)}};
 if(!data)return <Loading label="در حال دریافت ارزیابی..."/>;const ev=data.evaluation,active=ev?.indicators?.find((x:any)=>x.code===selectedIndicator)||null;
 return <div className="da-stack"><div className="da-toolbar"><select value={evaluationId} onChange={e=>{setEvaluationId(e.target.value);setSelectedIndicator("");void load(e.target.value)}}>{(data.periods||[]).map((p:any)=><option value={p.id} key={p.id}>{text(p.title,"دوره")} • {status(p.status)}</option>)}</select>{can(access,"staff.evaluations","create")&&<button className="da-btn soft" onClick={()=>setPeriod(true)}>دوره جدید</button>}{ev&&can(access,"staff.evaluations","update")&&ev.status!=="FINAL"&&<button className="da-btn primary" onClick={()=>void finalize()} disabled={busy}><BadgeCheck size={17}/>نهایی‌سازی ارزیابی</button>}</div>{ev?<><section className="da-metrics"><Metric label="وضعیت" value={status(ev.status)}/><Metric label="امتیاز نهایی" value={ev.finalScore==null?"—":fa(ev.finalScore)}/><Metric label="معیارها" value={fa(ev.totalCriteria||0)}/><Metric label="دوره‌ها" value={fa(data.periods?.length||0)}/></section><Card><div className="da-card-head"><div><h3>شاخص‌های ارزیابی</h3><p>برای مشاهده معیارهای هر شاخص روی کارت آن کلیک کنید.</p></div></div><div className="da-indicator-picker">{(ev.indicators||[]).map((ind:any)=><button type="button" key={ind.code} className={`da-indicator-card ${selectedIndicator===ind.code?"active":""}`} onClick={()=>setSelectedIndicator(ind.code)}><span className="da-indicator-code">{ind.code}</span><strong>{text(ind.title||ind.label,"شاخص ارزیابی")}</strong><small>{text(ind.description,"برای مشاهده معیارها انتخاب کنید")}</small><div><span>{fa((ind.criteria||[]).length)} معیار</span><b>{ind.score==null?"بدون امتیاز":`امتیاز ${fa(ind.score)}`}</b></div></button>)}</div></Card>{active?<Card className="da-indicator-detail"><div className="da-card-head"><div><h3>{active.code} • {text(active.title||active.label,"شاخص ارزیابی")}</h3><p>{text(active.description,"معیارهای این شاخص را امتیازدهی کنید.")}</p></div><span className="da-pill">{active.score==null?"—":fa(active.score)}</span></div><div className="da-criteria">{(active.criteria||[]).map((cr:any)=><div key={cr.code}><div><strong>{cr.code} • {text(cr.title||cr.label,"معیار")}</strong><small>{text(cr.description,"")}</small></div><div className="da-score-options">{[1,2,3,4,5].map(n=><label key={n}><input type="radio" name={`score-${cr.code}`} checked={Number(draft[cr.code]?.score)===n} onChange={()=>setDraft(prev=>({...prev,[cr.code]:{...(prev[cr.code]||{note:""}),score:n}}))} disabled={ev.status==="FINAL"||!can(access,"staff.evaluations","update")}/><span>{n}</span></label>)}</div><input className="da-note-input" value={draft[cr.code]?.note||""} onChange={e=>setDraft(prev=>({...prev,[cr.code]:{...(prev[cr.code]||{score:null}),note:e.target.value}}))} placeholder="یادداشت ارزیاب" disabled={ev.status==="FINAL"||!can(access,"staff.evaluations","update")}/></div>)}</div>{ev.status!=="FINAL"&&can(access,"staff.evaluations","update")&&<button className="da-btn primary" disabled={busy} onClick={()=>void saveIndicator(active)}>ذخیره شاخص {active.code}</button>}</Card>:<Empty title="یک شاخص را انتخاب کنید" description="کارت شاخص موردنظر را بزنید تا معیارهای همان شاخص باز شود."/>}</>:<Empty title="دوره ارزیابی وجود ندارد" description="برای این مراقب یک دوره جدید ایجاد کنید."/>}{period&&<Modal title="ایجاد دوره ارزیابی" onClose={()=>setPeriod(false)}><form className="da-form" onSubmit={createPeriod}><div className="da-form-grid"><label className="wide"><span>عنوان دوره</span><input name="title" required/></label><label><span>تاریخ شروع</span><input name="startDate" type="date" required/></label><label><span>تاریخ پایان</span><input name="endDate" type="date" required/></label></div><button className="da-btn primary" disabled={busy}>ایجاد دوره</button></form></Modal>}</div>
}

export function EvaluationsPage({access,notify}:{access:any;notify:Notify}){
 const [rows,setRows]=useState<any[]>([]),[page,setPage]=useState(1),[pagination,setPagination]=useState<any>({}),[query,setQuery]=useState(""),[selected,setSelected]=useState<any>(null),[loading,setLoading]=useState(false),[error,setError]=useState("");
 const load=async(next=page)=>{setLoading(true);setError("");try{const params=new URLSearchParams({page:String(next)});if(query.trim())params.set("q",query.trim());const p:any=await api(`/api/admin/caregivers-page?${params}`);setRows(listFromCaregiverPayload(p));setPagination(paginationFromCaregiverPayload(p));setPage(next)}catch(e:any){setRows([]);setError(e.message)}finally{setLoading(false)}};
 useEffect(()=>{void load(1)},[]);const totalPages=Number(pagination.totalPages||pagination.pages||1);
 return <div className="da-stack"><Card><form className="da-toolbar" onSubmit={e=>{e.preventDefault();void load(1)}}><div className="da-search"><Search size={18}/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="جست‌وجوی مراقب برای ارزیابی"/></div><button className="da-btn soft">جست‌وجو</button></form></Card>{error?<ErrorState message={error} retry={()=>load()}/>:loading?<Loading/>:<Card><div className="da-table-wrap"><table className="da-table"><thead><tr><th>مراقب</th><th>شماره پرونده</th><th>موبایل</th><th>سطح</th><th></th></tr></thead><tbody>{rows.map((c,i)=>{const id=caregiverIdOf(c)||String(i);return <tr key={id}><td><div className="da-person-cell"><span>{initials(c.fullName)}</span><strong>{text(c.fullName,"مراقب")}</strong></div></td><td>{text(c.membershipCode)}</td><td>{text(c.mobile)}</td><td>{text(c.professionalLevel,"—")}</td><td><button className="da-btn primary" onClick={()=>setSelected({...c,id})}><ClipboardCheck size={16}/>ارزیابی</button></td></tr>})}</tbody></table></div><div className="da-pager"><button disabled={page<=1} onClick={()=>load(page-1)}>قبلی</button><span>{fa(pagination.total||rows.length)} پرونده</span><button disabled={page>=totalPages} onClick={()=>load(page+1)}>بعدی</button></div></Card>}{selected&&<Modal title={`ارزیابی ${text(selected.fullName,"مراقب")}`} subtitle="ابتدا شاخص را انتخاب کنید؛ سپس فقط معیارهای همان شاخص برای امتیازدهی باز می‌شود." onClose={()=>setSelected(null)} wide><EvaluationEditor caregiver={selected} access={access} notify={notify}/></Modal>}</div>
}
